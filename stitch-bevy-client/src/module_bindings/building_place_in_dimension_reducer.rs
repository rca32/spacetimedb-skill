// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct BuildingPlaceInDimensionArgs {
    pub building_id: u64,
    pub region_id: u64,
    pub dimension_id: u32,
    pub hex_x: i32,
    pub hex_z: i32,
    pub required_item_def_id: u64,
    pub required_item_qty: u32,
    pub build_required: u32,
}

impl From<BuildingPlaceInDimensionArgs> for super::Reducer {
    fn from(args: BuildingPlaceInDimensionArgs) -> Self {
        Self::BuildingPlaceInDimension {
            building_id: args.building_id,
            region_id: args.region_id,
            dimension_id: args.dimension_id,
            hex_x: args.hex_x,
            hex_z: args.hex_z,
            required_item_def_id: args.required_item_def_id,
            required_item_qty: args.required_item_qty,
            build_required: args.build_required,
        }
    }
}

impl __sdk::InModule for BuildingPlaceInDimensionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `building_place_in_dimension`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait building_place_in_dimension {
    /// Request that the remote module invoke the reducer `building_place_in_dimension` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`building_place_in_dimension:building_place_in_dimension_then`] to run a callback after the reducer completes.
    fn building_place_in_dimension(
        &self,
        building_id: u64,
        region_id: u64,
        dimension_id: u32,
        hex_x: i32,
        hex_z: i32,
        required_item_def_id: u64,
        required_item_qty: u32,
        build_required: u32,
    ) -> __sdk::Result<()> {
        self.building_place_in_dimension_then(
            building_id,
            region_id,
            dimension_id,
            hex_x,
            hex_z,
            required_item_def_id,
            required_item_qty,
            build_required,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `building_place_in_dimension` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn building_place_in_dimension_then(
        &self,
        building_id: u64,
        region_id: u64,
        dimension_id: u32,
        hex_x: i32,
        hex_z: i32,
        required_item_def_id: u64,
        required_item_qty: u32,
        build_required: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl building_place_in_dimension for super::RemoteReducers {
    fn building_place_in_dimension_then(
        &self,
        building_id: u64,
        region_id: u64,
        dimension_id: u32,
        hex_x: i32,
        hex_z: i32,
        required_item_def_id: u64,
        required_item_qty: u32,
        build_required: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            BuildingPlaceInDimensionArgs {
                building_id,
                region_id,
                dimension_id,
                hex_x,
                hex_z,
                required_item_def_id,
                required_item_qty,
                build_required,
            },
            callback,
        )
    }
}
