// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct BuildingValidatePreviewArgs {
    pub request_id: String,
    pub building_def_id: u64,
    pub region_id: u64,
    pub dimension_id: u32,
    pub hex_x: i32,
    pub hex_z: i32,
    pub facing: u8,
}

impl From<BuildingValidatePreviewArgs> for super::Reducer {
    fn from(args: BuildingValidatePreviewArgs) -> Self {
        Self::BuildingValidatePreview {
            request_id: args.request_id,
            building_def_id: args.building_def_id,
            region_id: args.region_id,
            dimension_id: args.dimension_id,
            hex_x: args.hex_x,
            hex_z: args.hex_z,
            facing: args.facing,
        }
    }
}

impl __sdk::InModule for BuildingValidatePreviewArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `building_validate_preview`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait building_validate_preview {
    /// Request that the remote module invoke the reducer `building_validate_preview` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`building_validate_preview:building_validate_preview_then`] to run a callback after the reducer completes.
    fn building_validate_preview(
        &self,
        request_id: String,
        building_def_id: u64,
        region_id: u64,
        dimension_id: u32,
        hex_x: i32,
        hex_z: i32,
        facing: u8,
    ) -> __sdk::Result<()> {
        self.building_validate_preview_then(
            request_id,
            building_def_id,
            region_id,
            dimension_id,
            hex_x,
            hex_z,
            facing,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `building_validate_preview` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn building_validate_preview_then(
        &self,
        request_id: String,
        building_def_id: u64,
        region_id: u64,
        dimension_id: u32,
        hex_x: i32,
        hex_z: i32,
        facing: u8,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl building_validate_preview for super::RemoteReducers {
    fn building_validate_preview_then(
        &self,
        request_id: String,
        building_def_id: u64,
        region_id: u64,
        dimension_id: u32,
        hex_x: i32,
        hex_z: i32,
        facing: u8,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            BuildingValidatePreviewArgs {
                request_id,
                building_def_id,
                region_id,
                dimension_id,
                hex_x,
                hex_z,
                facing,
            },
            callback,
        )
    }
}
