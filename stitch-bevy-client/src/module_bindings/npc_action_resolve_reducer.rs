// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct NpcActionResolveArgs {
    pub request_id: String,
    pub status: u8,
    pub summary: String,
    pub token_in: u32,
    pub token_out: u32,
    pub cost_microunits: u64,
}

impl From<NpcActionResolveArgs> for super::Reducer {
    fn from(args: NpcActionResolveArgs) -> Self {
        Self::NpcActionResolve {
            request_id: args.request_id,
            status: args.status,
            summary: args.summary,
            token_in: args.token_in,
            token_out: args.token_out,
            cost_microunits: args.cost_microunits,
        }
    }
}

impl __sdk::InModule for NpcActionResolveArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `npc_action_resolve`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait npc_action_resolve {
    /// Request that the remote module invoke the reducer `npc_action_resolve` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`npc_action_resolve:npc_action_resolve_then`] to run a callback after the reducer completes.
    fn npc_action_resolve(
        &self,
        request_id: String,
        status: u8,
        summary: String,
        token_in: u32,
        token_out: u32,
        cost_microunits: u64,
    ) -> __sdk::Result<()> {
        self.npc_action_resolve_then(
            request_id,
            status,
            summary,
            token_in,
            token_out,
            cost_microunits,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `npc_action_resolve` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn npc_action_resolve_then(
        &self,
        request_id: String,
        status: u8,
        summary: String,
        token_in: u32,
        token_out: u32,
        cost_microunits: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl npc_action_resolve for super::RemoteReducers {
    fn npc_action_resolve_then(
        &self,
        request_id: String,
        status: u8,
        summary: String,
        token_in: u32,
        token_out: u32,
        cost_microunits: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            NpcActionResolveArgs {
                request_id,
                status,
                summary,
                token_in,
                token_out,
                cost_microunits,
            },
            callback,
        )
    }
}
