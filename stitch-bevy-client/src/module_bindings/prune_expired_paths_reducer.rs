// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct PruneExpiredPathsArgs {
    pub keep_rows_per_identity: u32,
}

impl From<PruneExpiredPathsArgs> for super::Reducer {
    fn from(args: PruneExpiredPathsArgs) -> Self {
        Self::PruneExpiredPaths {
            keep_rows_per_identity: args.keep_rows_per_identity,
        }
    }
}

impl __sdk::InModule for PruneExpiredPathsArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `prune_expired_paths`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait prune_expired_paths {
    /// Request that the remote module invoke the reducer `prune_expired_paths` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`prune_expired_paths:prune_expired_paths_then`] to run a callback after the reducer completes.
    fn prune_expired_paths(&self, keep_rows_per_identity: u32) -> __sdk::Result<()> {
        self.prune_expired_paths_then(keep_rows_per_identity, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `prune_expired_paths` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn prune_expired_paths_then(
        &self,
        keep_rows_per_identity: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl prune_expired_paths for super::RemoteReducers {
    fn prune_expired_paths_then(
        &self,
        keep_rows_per_identity: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            PruneExpiredPathsArgs {
                keep_rows_per_identity,
            },
            callback,
        )
    }
}
