// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RequestPathArgs {
    pub region_id: u64,
    pub start_hex_x: i32,
    pub start_hex_z: i32,
    pub goal_hex_x: i32,
    pub goal_hex_z: i32,
    pub node_limit: u32,
}

impl From<RequestPathArgs> for super::Reducer {
    fn from(args: RequestPathArgs) -> Self {
        Self::RequestPath {
            region_id: args.region_id,
            start_hex_x: args.start_hex_x,
            start_hex_z: args.start_hex_z,
            goal_hex_x: args.goal_hex_x,
            goal_hex_z: args.goal_hex_z,
            node_limit: args.node_limit,
        }
    }
}

impl __sdk::InModule for RequestPathArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `request_path`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait request_path {
    /// Request that the remote module invoke the reducer `request_path` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`request_path:request_path_then`] to run a callback after the reducer completes.
    fn request_path(
        &self,
        region_id: u64,
        start_hex_x: i32,
        start_hex_z: i32,
        goal_hex_x: i32,
        goal_hex_z: i32,
        node_limit: u32,
    ) -> __sdk::Result<()> {
        self.request_path_then(
            region_id,
            start_hex_x,
            start_hex_z,
            goal_hex_x,
            goal_hex_z,
            node_limit,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `request_path` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn request_path_then(
        &self,
        region_id: u64,
        start_hex_x: i32,
        start_hex_z: i32,
        goal_hex_x: i32,
        goal_hex_z: i32,
        node_limit: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl request_path for super::RemoteReducers {
    fn request_path_then(
        &self,
        region_id: u64,
        start_hex_x: i32,
        start_hex_z: i32,
        goal_hex_x: i32,
        goal_hex_z: i32,
        node_limit: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RequestPathArgs {
                region_id,
                start_hex_x,
                start_hex_z,
                goal_hex_x,
                goal_hex_z,
                node_limit,
            },
            callback,
        )
    }
}
