// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpsertNpcAnchorStateArgs {
    pub anchor_id: u64,
    pub region_id: u64,
    pub hex_x: i32,
    pub hex_z: i32,
    pub anchor_kind: u8,
    pub is_active: bool,
}

impl From<UpsertNpcAnchorStateArgs> for super::Reducer {
    fn from(args: UpsertNpcAnchorStateArgs) -> Self {
        Self::UpsertNpcAnchorState {
            anchor_id: args.anchor_id,
            region_id: args.region_id,
            hex_x: args.hex_x,
            hex_z: args.hex_z,
            anchor_kind: args.anchor_kind,
            is_active: args.is_active,
        }
    }
}

impl __sdk::InModule for UpsertNpcAnchorStateArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `upsert_npc_anchor_state`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait upsert_npc_anchor_state {
    /// Request that the remote module invoke the reducer `upsert_npc_anchor_state` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`upsert_npc_anchor_state:upsert_npc_anchor_state_then`] to run a callback after the reducer completes.
    fn upsert_npc_anchor_state(
        &self,
        anchor_id: u64,
        region_id: u64,
        hex_x: i32,
        hex_z: i32,
        anchor_kind: u8,
        is_active: bool,
    ) -> __sdk::Result<()> {
        self.upsert_npc_anchor_state_then(
            anchor_id,
            region_id,
            hex_x,
            hex_z,
            anchor_kind,
            is_active,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `upsert_npc_anchor_state` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn upsert_npc_anchor_state_then(
        &self,
        anchor_id: u64,
        region_id: u64,
        hex_x: i32,
        hex_z: i32,
        anchor_kind: u8,
        is_active: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl upsert_npc_anchor_state for super::RemoteReducers {
    fn upsert_npc_anchor_state_then(
        &self,
        anchor_id: u64,
        region_id: u64,
        hex_x: i32,
        hex_z: i32,
        anchor_kind: u8,
        is_active: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpsertNpcAnchorStateArgs {
                anchor_id,
                region_id,
                hex_x,
                hex_z,
                anchor_kind,
                is_active,
            },
            callback,
        )
    }
}
