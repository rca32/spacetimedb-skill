// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct GenerateWorldInDimensionArgs {
    pub region_id: u64,
    pub dimension_id: u32,
    pub seed: u64,
    pub size_x_chunks: i32,
    pub size_y_chunks: i32,
    pub overwrite: bool,
}

impl From<GenerateWorldInDimensionArgs> for super::Reducer {
    fn from(args: GenerateWorldInDimensionArgs) -> Self {
        Self::GenerateWorldInDimension {
            region_id: args.region_id,
            dimension_id: args.dimension_id,
            seed: args.seed,
            size_x_chunks: args.size_x_chunks,
            size_y_chunks: args.size_y_chunks,
            overwrite: args.overwrite,
        }
    }
}

impl __sdk::InModule for GenerateWorldInDimensionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `generate_world_in_dimension`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait generate_world_in_dimension {
    /// Request that the remote module invoke the reducer `generate_world_in_dimension` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`generate_world_in_dimension:generate_world_in_dimension_then`] to run a callback after the reducer completes.
    fn generate_world_in_dimension(
        &self,
        region_id: u64,
        dimension_id: u32,
        seed: u64,
        size_x_chunks: i32,
        size_y_chunks: i32,
        overwrite: bool,
    ) -> __sdk::Result<()> {
        self.generate_world_in_dimension_then(
            region_id,
            dimension_id,
            seed,
            size_x_chunks,
            size_y_chunks,
            overwrite,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `generate_world_in_dimension` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn generate_world_in_dimension_then(
        &self,
        region_id: u64,
        dimension_id: u32,
        seed: u64,
        size_x_chunks: i32,
        size_y_chunks: i32,
        overwrite: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl generate_world_in_dimension for super::RemoteReducers {
    fn generate_world_in_dimension_then(
        &self,
        region_id: u64,
        dimension_id: u32,
        seed: u64,
        size_x_chunks: i32,
        size_y_chunks: i32,
        overwrite: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            GenerateWorldInDimensionArgs {
                region_id,
                dimension_id,
                seed,
                size_x_chunks,
                size_y_chunks,
                overwrite,
            },
            callback,
        )
    }
}
