// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SubmitMotionIntentArgs {
    pub intent_id: String,
    pub region_id: u64,
    pub dimension_id: u32,
    pub frame_no: u64,
    pub input_x: f32,
    pub input_z: f32,
    pub requested_speed: f32,
    pub jump: bool,
}

impl From<SubmitMotionIntentArgs> for super::Reducer {
    fn from(args: SubmitMotionIntentArgs) -> Self {
        Self::SubmitMotionIntent {
            intent_id: args.intent_id,
            region_id: args.region_id,
            dimension_id: args.dimension_id,
            frame_no: args.frame_no,
            input_x: args.input_x,
            input_z: args.input_z,
            requested_speed: args.requested_speed,
            jump: args.jump,
        }
    }
}

impl __sdk::InModule for SubmitMotionIntentArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `submit_motion_intent`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait submit_motion_intent {
    /// Request that the remote module invoke the reducer `submit_motion_intent` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`submit_motion_intent:submit_motion_intent_then`] to run a callback after the reducer completes.
    fn submit_motion_intent(
        &self,
        intent_id: String,
        region_id: u64,
        dimension_id: u32,
        frame_no: u64,
        input_x: f32,
        input_z: f32,
        requested_speed: f32,
        jump: bool,
    ) -> __sdk::Result<()> {
        self.submit_motion_intent_then(
            intent_id,
            region_id,
            dimension_id,
            frame_no,
            input_x,
            input_z,
            requested_speed,
            jump,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `submit_motion_intent` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn submit_motion_intent_then(
        &self,
        intent_id: String,
        region_id: u64,
        dimension_id: u32,
        frame_no: u64,
        input_x: f32,
        input_z: f32,
        requested_speed: f32,
        jump: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl submit_motion_intent for super::RemoteReducers {
    fn submit_motion_intent_then(
        &self,
        intent_id: String,
        region_id: u64,
        dimension_id: u32,
        frame_no: u64,
        input_x: f32,
        input_z: f32,
        requested_speed: f32,
        jump: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SubmitMotionIntentArgs {
                intent_id,
                region_id,
                dimension_id,
                frame_no,
                input_x,
                input_z,
                requested_speed,
                jump,
            },
            callback,
        )
    }
}
