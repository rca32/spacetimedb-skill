// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct TradeItemAddArgs {
    pub session_id: String,
    pub item_instance_id: u64,
    pub quantity: u32,
}

impl From<TradeItemAddArgs> for super::Reducer {
    fn from(args: TradeItemAddArgs) -> Self {
        Self::TradeItemAdd {
            session_id: args.session_id,
            item_instance_id: args.item_instance_id,
            quantity: args.quantity,
        }
    }
}

impl __sdk::InModule for TradeItemAddArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `trade_item_add`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait trade_item_add {
    /// Request that the remote module invoke the reducer `trade_item_add` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`trade_item_add:trade_item_add_then`] to run a callback after the reducer completes.
    fn trade_item_add(
        &self,
        session_id: String,
        item_instance_id: u64,
        quantity: u32,
    ) -> __sdk::Result<()> {
        self.trade_item_add_then(session_id, item_instance_id, quantity, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `trade_item_add` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn trade_item_add_then(
        &self,
        session_id: String,
        item_instance_id: u64,
        quantity: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl trade_item_add for super::RemoteReducers {
    fn trade_item_add_then(
        &self,
        session_id: String,
        item_instance_id: u64,
        quantity: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            TradeItemAddArgs {
                session_id,
                item_instance_id,
                quantity,
            },
            callback,
        )
    }
}
