// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct GetChunkPayloadArgs {
    pub region_id: u64,
    pub dimension_id: u32,
    pub chunk_x: i32,
    pub chunk_y: i32,
}

impl From<GetChunkPayloadArgs> for super::Reducer {
    fn from(args: GetChunkPayloadArgs) -> Self {
        Self::GetChunkPayload {
            region_id: args.region_id,
            dimension_id: args.dimension_id,
            chunk_x: args.chunk_x,
            chunk_y: args.chunk_y,
        }
    }
}

impl __sdk::InModule for GetChunkPayloadArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `get_chunk_payload`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait get_chunk_payload {
    /// Request that the remote module invoke the reducer `get_chunk_payload` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`get_chunk_payload:get_chunk_payload_then`] to run a callback after the reducer completes.
    fn get_chunk_payload(
        &self,
        region_id: u64,
        dimension_id: u32,
        chunk_x: i32,
        chunk_y: i32,
    ) -> __sdk::Result<()> {
        self.get_chunk_payload_then(region_id, dimension_id, chunk_x, chunk_y, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `get_chunk_payload` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn get_chunk_payload_then(
        &self,
        region_id: u64,
        dimension_id: u32,
        chunk_x: i32,
        chunk_y: i32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl get_chunk_payload for super::RemoteReducers {
    fn get_chunk_payload_then(
        &self,
        region_id: u64,
        dimension_id: u32,
        chunk_x: i32,
        chunk_y: i32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            GetChunkPayloadArgs {
                region_id,
                dimension_id,
                chunk_x,
                chunk_y,
            },
            callback,
        )
    }
}
