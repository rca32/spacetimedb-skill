// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpsertNpcTradeOrderDefArgs {
    pub order_def_id: u64,
    pub npc_type: u8,
    pub item_def_id: u64,
    pub side: u8,
    pub min_quantity: u32,
    pub max_quantity: u32,
    pub base_unit_price: u64,
    pub weight: u16,
    pub always_offered: bool,
    pub enabled: bool,
}

impl From<UpsertNpcTradeOrderDefArgs> for super::Reducer {
    fn from(args: UpsertNpcTradeOrderDefArgs) -> Self {
        Self::UpsertNpcTradeOrderDef {
            order_def_id: args.order_def_id,
            npc_type: args.npc_type,
            item_def_id: args.item_def_id,
            side: args.side,
            min_quantity: args.min_quantity,
            max_quantity: args.max_quantity,
            base_unit_price: args.base_unit_price,
            weight: args.weight,
            always_offered: args.always_offered,
            enabled: args.enabled,
        }
    }
}

impl __sdk::InModule for UpsertNpcTradeOrderDefArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `upsert_npc_trade_order_def`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait upsert_npc_trade_order_def {
    /// Request that the remote module invoke the reducer `upsert_npc_trade_order_def` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`upsert_npc_trade_order_def:upsert_npc_trade_order_def_then`] to run a callback after the reducer completes.
    fn upsert_npc_trade_order_def(
        &self,
        order_def_id: u64,
        npc_type: u8,
        item_def_id: u64,
        side: u8,
        min_quantity: u32,
        max_quantity: u32,
        base_unit_price: u64,
        weight: u16,
        always_offered: bool,
        enabled: bool,
    ) -> __sdk::Result<()> {
        self.upsert_npc_trade_order_def_then(
            order_def_id,
            npc_type,
            item_def_id,
            side,
            min_quantity,
            max_quantity,
            base_unit_price,
            weight,
            always_offered,
            enabled,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `upsert_npc_trade_order_def` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn upsert_npc_trade_order_def_then(
        &self,
        order_def_id: u64,
        npc_type: u8,
        item_def_id: u64,
        side: u8,
        min_quantity: u32,
        max_quantity: u32,
        base_unit_price: u64,
        weight: u16,
        always_offered: bool,
        enabled: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl upsert_npc_trade_order_def for super::RemoteReducers {
    fn upsert_npc_trade_order_def_then(
        &self,
        order_def_id: u64,
        npc_type: u8,
        item_def_id: u64,
        side: u8,
        min_quantity: u32,
        max_quantity: u32,
        base_unit_price: u64,
        weight: u16,
        always_offered: bool,
        enabled: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpsertNpcTradeOrderDefArgs {
                order_def_id,
                npc_type,
                item_def_id,
                side,
                min_quantity,
                max_quantity,
                base_unit_price,
                weight,
                always_offered,
                enabled,
            },
            callback,
        )
    }
}
