# 2026-03-12 Progress

## 진행 요약

- `AGENTS.md` 지침 확인
- `2DGameEngine/*` 기존 런타임/동기화/로드맵 문서 검토
- SpacetimeDB 스킬과 client integration 기준 확인
- `DESIGN/*`의 테이블/뷰 기준과 `stitch-server` 연동 포인트를 재정리
- 요청한 9개 우선순위를 기준으로 `web-client` 실행 계획 문서 추가
- 오늘 작업의 진행 기록과 작업 후 기록 초안 작성

## 이번 작업에서 정리한 핵심 포인트

- 이번 요청은 새 아키텍처 문서 전체를 다시 쓰는 것이 아니라, 기존 `2DGameEngine` 문서를 실제 구현 우선순위로 재배열하는 작업이다.
- `web-client`의 첫 완료 기준은 Pixi 화면보다 먼저 `player_session_view` 기반 세션 baseline 확보에 둬야 한다.
- Spacetime 연결은 selective subscription과 `subscription applied` barrier를 기본 전제로 둬야 한다.
- terrain 렌더 단계는 `DESIGN`의 `terrain_chunk`를 기준으로 삼되, 현재 `stitch-server`의 `terrain_chunk_payload` helper를 흡수할 adapter 경계를 둬야 한다.
- 인벤토리와 건설 preview는 optimistic UX를 최소화하고 projection/view 기준 authoritative overwrite를 유지해야 한다.
- 전투/NPC/소셜은 world sync와 별도의 도메인 store를 유지하는 편이 이후 확장에 안전하다.

## 생성/수정한 산출물

- `2DGameEngine/08-web-client-priority-execution-plan.md`
- `2DGameEngine/README.md`
- `prompts/20260312_progress.md`
- `prompts/20260312_workbook.md`

## 구현 시작 산출물

- `web-client/` 초기 프로젝트 생성
- Pixi bootstrap과 브라우저 app shell 추가
- `GameRuntime`, `FrameClock`, `AuthoritativeStore`, `EventLogStore` 골격 추가
- `SpacetimeClient`, `SubscriptionCoordinator`, `ReducerGateway` 초기 연결층 추가
- `src/module_bindings/index.ts` placeholder와 bindings generation script 추가
- AssetPack config 초안과 seed asset staging script 추가

## 추가 구현 산출물

- bindings 생성 기본 모듈 경로를 `stitch-server/crates/game_server`로 수정
- `terrain_chunk_stream`, `terrain_chunk_payload`, `player_wallet_view`를 bootstrap subscription 그룹에 추가
- `AuthoritativeStore.getRows()`와 chunk/slot 복합 키 처리를 추가
- Pixi world placeholder 렌더러 추가
- inventory projection을 읽는 DOM HUD 카드 추가
- `terrain_chunk_payload.cell_payload_bytes`와 `terrain_chunk.cell_payload`를 모두 읽는 terrain decoder 추가
- seed asset 스테이징 스크립트와 Spacetime bindings generation 스크립트 실행 시작
- `package.json`의 asset/bindings 스크립트를 `bash` 실행 기준으로 수정
- `bun install`을 완료해 `web-client` 의존성과 lockfile을 복구
- `bun run assets:stage` 재실행으로 `public/assets/seed/kenney/*` 정적 파일 복사를 완료
- `bun run spacetime:generate` 재실행으로 `src/module_bindings/*` 실제 generated bindings를 생성
- `physics_state`, `player_movement_feedback_view`, `server_correction`를 movement bootstrap 구독 그룹에 추가
- `row-access.ts`를 추가해 generated camelCase row와 기존 snake_case 접근을 함께 수용하도록 정리
- `movement-prediction-runtime.ts`를 `sync_client_frame`, `submit_motion_intent`, `ack_server_correction` 흐름으로 확장
- `bun run build`로 현재 `web-client` 프로덕션 빌드 통과를 확인
- `stage-seed-assets.sh`를 `assets-src -> public` fallback 기준으로 보강해 `tiny-town`, `top-down-shooter` 정적 파일 누락을 복구
- `bunfig.toml`에 `linker = "hoisted"`를 추가해 `vite` 로컬 바이너리 해석이 안정적으로 동작하게 수정
- `SubscriptionCoordinator`를 세션 이후 `region_id`/`dimension_id` + anchor chunk 기반 world 재구독 구조로 확장
- `SeedAssetRuntime`와 `WorldRenderer`를 seed texture 기반 terrain/resource/building/actor/NPC 렌더링 쪽으로 보강
- `SeedAssetRuntime`를 biome/building/resource별 texture pool preload 구조로 확장하고, `WorldRenderer`에 deterministic frame 선택 규칙을 추가
- `SubscriptionCoordinator`가 anchor chunk 변경 시 `request_chunks_for_aoi` reducer를 positional args로 호출하도록 연결
- `MovementPredictionRuntime`의 `sync_client_frame`, `submit_motion_intent`, `ack_server_correction` reducer 호출을 generated signature 기준 positional args로 수정
- `InteractionStore`, `BuildingPreviewHud`, `ReducerDispatchHud`를 추가해 building preview validate/place, AOI request, chunk drain quick action UI를 연결
- `InventoryHud`를 drag ghost + slot drop 기반 `item_stack_move` dispatch UI로 확장
- `bunx tsc --noEmit`를 실행해 로컬 코드 타입 오류를 정리했고, 남은 오류가 generated `src/module_bindings/index.ts` schema typing 1건뿐임을 확인
- generated `src/module_bindings/index.ts`에 object-form schema codegen을 수용하는 local wrapper를 추가해 브라우저 런타임 부팅 블로커를 제거
- `agent-browser`로 `http://127.0.0.1:5173`를 열어 HUD/canvas 렌더와 interactive ref를 확인했고, `input-prompts` prompt texture 경로 오류를 수정
- `biome_gen_def`, `resource_gen_def`, `building_def`를 world bootstrap 구독 그룹에 추가했다.
- `WorldRenderer`의 terrain/resource/building seed frame 선택을 def 테이블 metadata 우선 구조로 치환하고, 데이터가 없을 때만 heuristic fallback을 타게 정리했다.
- `bunx tsc --noEmit`, `bun run build`를 다시 실행해 metadata 기반 렌더 치환 이후에도 컴파일/빌드가 유지됨을 확인했다.
- `agent-browser`로 `http://127.0.0.1:5174`를 열어 페이지 로딩, canvas 존재, 콘솔/페이지 에러 부재를 다시 확인했다. 콘솔에는 Vite 연결 로그와 Chromium WebGL 성능 경고만 남았다.
- building preview HUD에 `pointer target` 액션과 claim/permission 요약 라인을 추가했다.
- `GameRuntime` canvas pointer 이벤트를 연결해 preview targeting 중 마우스 이동으로 hex 좌표를 갱신하고 클릭으로 target을 고정하도록 만들었다.
- `claim_state`를 읽는 shared helper를 추가하고, `WorldRenderer`에 claim radius overlay/owner-highlight/permission-block tint를 붙였다.
- `agent-browser`로 `http://127.0.0.1:5174`를 다시 열어 `pointer target` 버튼 노출과 페이지 에러 부재를 확인했다. 현재 dev 기본 설정이 `connectOnBoot=false`라 실제 session 기반 picking round-trip은 브라우저에서 재현되지 않았다.
- SDK 연결로 테스트 identity/token을 생성하고 `account_bootstrap`, `sign_in(1)`을 호출해 `player_session_view` 세션 row를 실제로 만들었다.
- 로컬 전용 `web-client/.env.local`에 token과 `VITE_CONNECT_ON_BOOT=true`를 넣어 연결 모드 dev server를 `http://127.0.0.1:5180`에서 재실행했다.
- `AuthoritativeStore`의 fallback key 추론이 def row `BigInt`에서 터지던 문제를 primary key 후보 확장으로 수정했다.
- `SubscriptionCoordinator`의 `request_chunks_for_aoi` 호출을 generated object-form schema에 맞게 바꿔 `Cannot convert undefined to a BigInt` 경고를 제거했다.
- `agent-browser`와 서버 SQL로 live world loading을 확인했다. 브라우저 DOM 텍스트 기준 `player_session_view=1`, `terrain_chunk_stream=49`, `terrain_chunk_payload=49`, `transform_state=1`, `resource_node=1`, `npc_state_stream=1`이 들어왔다.

## 다음 추천 작업

- inventory drag를 cross-container move/split stack/locked slot reason 표시까지 확장
- biome/resource/building def만으로는 표현되지 않는 아트 규칙을 전용 render metadata 테이블 또는 asset tag 규약으로 승격해 heuristic fallback을 제거
- building preview pointer picking을 live session 환경에서 reducer round-trip과 함께 검증하고, preview click 시 auto-validate 여부를 결정
- claim overlay에 footprint/tile occupancy까지 함께 표시해 invalid reason을 월드 셀 단위로 드러내기
- live connection용 임시 token/env를 영구 개발 플로우로 정리할지, sign-in bootstrap UX를 클라이언트 자체에 넣을지 결정

## 현재 작업 상세 정리

- `web-client` 프로젝트를 생성하고 `bun` + TypeScript + Vite + PixiJS 기반 부트스트랩을 추가했다.
- `GameRuntime`, `FrameClock`, `SpacetimeClient`, `SubscriptionCoordinator`, `ReducerGateway`, `AuthoritativeStore`, `EventLogStore` 골격을 구현했다.
- Pixi canvas와 DOM HUD를 동시에 올리는 브라우저 shell 구조를 만들었다.
- bootstrap subscription에 `player_session_view`, `terrain_chunk`, `terrain_chunk_stream`, `terrain_chunk_payload`, `transform_state`, `resource_node`, `building_state`, `claim_state`, `npc_state`, `player_inventory_container_view`, `player_inventory_slot_view`, `player_inventory_item_view`, `player_wallet_view`를 연결했다.
- `terrain-payload-decoder.ts`를 추가해 `terrain_chunk_payload.cell_payload_bytes`와 `terrain_chunk.cell_payload`를 모두 읽는 디코더를 구현했다.
- `world-renderer.ts`를 추가해 terrain chunk, payload preview cell, resource/building/npc placeholder, `transform_state` actor marker를 Pixi에 그리도록 구현했다.
- `inventory-hud.ts`를 추가해 `player_session_view`, `player_wallet_view`, `player_inventory_*_view` 기반 bootstrap HUD를 만들었다.
- `movement` 쪽은 `input-frame-buffer.ts`, `movement-intent-buffer.ts`, `movement-prediction-runtime.ts`, `movement-hud.ts`를 추가해 WASD+Shift 입력, predicted/auth state 비교, pending intent 수 표시까지 구현했다.
- seed asset preload를 위해 `seed-asset-runtime.ts`를 추가했고, top-down-shooter sprite sheet PNG와 input prompt PNG를 로드하도록 연결했다.
- `stage-seed-assets.sh`를 확장해 `assets-src/seed`뿐 아니라 `public/assets/seed`로도 복사되게 수정했다.
- `generate-bindings.sh` 기본 모듈 경로를 `stitch-server/crates/game_server`로 수정했다.
- 생성된 bindings가 실제로 camelCase row 필드를 노출한다는 점을 확인하고, `AuthoritativeStore`, terrain decoder, world renderer, inventory HUD를 generated row naming에 맞게 보강했다.
- `SubscriptionCoordinator`는 세션/개인 projection/movement projection에 대해 identity 기반 selective query를 사용하도록 바꿨다.
- `MovementPredictionRuntime`는 `physics_state` authoritative 위치, `player_movement_feedback_view` 승인/거절 결과, `server_correction` 강제 보정과 `ack_server_correction` 응답을 처리하도록 확장했다.
- `stage-seed-assets.sh`는 assetdirectory 원본이 일부 비어 있어도 이미 확보된 `assets-src/seed`를 기준으로 `public/assets/seed`를 채우도록 수정했다.
- `SeedAssetRuntime`는 `tiny-town`/`top-down-shooter` seed texture를 직접 preload하고, `WorldRenderer`는 도형 placeholder 대신 seed texture sprite를 우선 사용하도록 바꿨다.
- `SubscriptionCoordinator`는 세션 row와 자기 `physics_state`를 읽어 world query를 `region_id`/`dimension_id` 기준으로 붙인 뒤, anchor chunk가 잡히면 active/preload 범위로 다시 좁히도록 바꿨다.
- `SeedAssetRuntime`는 단일 texture가 아니라 biome/building/resource category별 texture pool을 preload하도록 바꿨고, `WorldRenderer`는 `biome_id`, `water_ratio_permille`, `resource_type`, `required_item_def_id`, `build_required`, `state`를 기준으로 deterministic seed frame을 고르도록 보강했다.
- `SubscriptionCoordinator`는 anchor chunk가 갱신될 때마다 `request_chunks_for_aoi(regionId, dimensionId, minChunkX, maxChunkX, minChunkY, maxChunkY)`를 실제 generated reducer signature 순서대로 호출하도록 수정했다.
- `MovementPredictionRuntime`는 기존 object payload 방식이 아니라 generated reducer signature 순서를 따르는 positional args 방식으로 `sync_client_frame`, `submit_motion_intent`, `ack_server_correction`를 호출하게 수정했다.
- `InteractionStore`를 추가해 building preview 상태를 중앙 저장하고, `WorldRenderer`가 preview ghost overlay를 직접 읽어 그리게 했다.
- `inventory-hud.ts`는 slot 카드, move quantity 입력, drag ghost, same-container slot drop 기반 `item_stack_move` dispatch UI로 확장했다.
- `building-preview-hud.ts`는 self anchor, hex 좌표 draft, facing 회전, `building_validate_preview`, `building_place_from_preview` 호출과 `building_preview_feedback_view` authoritative feedback 동기화를 담당하도록 추가했다.
- `reducer-dispatch-hud.ts`는 `request_chunks_for_aoi`, `drain_chunk_generation_queue_now`, preview 재검증/배치 quick action 버튼과 현재 session/anchor/preview 상태 요약을 제공하도록 추가했다.
- `game-runtime.ts`는 `InteractionStore`, `BuildingPreviewHud`, `ReducerDispatchHud`를 런타임에 주입하고, `SubscriptionCoordinator`에 `ReducerGateway`를 넘기도록 정리했다.
- `styles.css`는 interactive HUD 카드, slot grid, 버튼, 입력 필드, drag ghost를 위한 스타일을 추가했다.
- generated `src/module_bindings/index.ts`는 현재 codegen 출력이 `schema({...})` object-form을 사용해 브라우저에서 터지던 문제를 local wrapper로 우회하도록 보강했다.
- `agent-browser` 검증으로 페이지가 실제로 뜨는지 확인했고, 브라우저에서 드러난 `input-prompts` prompt texture preload 실패는 `Generic/Default/generic_button.png` 경로로 수정했다.
- `SubscriptionCoordinator` world 그룹은 `biome_gen_def`, `resource_gen_def`, `building_def`까지 함께 구독하도록 확장해 renderer가 seed lookup에 필요한 def rows를 authoritative cache에서 직접 읽게 했다.
- `WorldRenderer`는 `biome_gen_def.name`, `resource_gen_def`의 elevation/water/noise 제약, `building_def`의 `build_required`/`footprint_radius`를 기준으로 texture pool group을 고르고, def row가 없을 때만 기존 biome/resource/building heuristic로 내려가게 수정했다.
- `agent-browser` 재검증으로 `http://127.0.0.1:5174`에서 `canvas` 1개와 정상 title을 확인했고, 페이지 에러는 없으며 콘솔에는 Vite 연결 로그와 WebGL `ReadPixels` 성능 경고만 남는 것을 확인했다.
- `claim-coverage.ts`를 추가해 claim 포함 판정과 smallest-radius match lookup을 공용화했다.
- `BuildingPreviewHud`는 target claim/permission 요약을 표시하고, `pointer target` 액션으로 canvas picking 모드에 들어갈 수 있게 보강했다.
- `GameRuntime`는 canvas `pointermove`/`pointerdown`을 preview state에 연결해 pointer targeting 중 hex 좌표를 갱신하고 클릭 시 target을 고정하게 만들었다.
- `WorldRenderer`는 `claim_state` 원형 overlay, self-claim highlight, `no_build_permission_in_claim` 차단 tint를 추가해 preview invalid reason을 월드에 직접 투영하게 수정했다.
- 테스트용 SDK 연결에서 받은 token으로 `account_bootstrap("web-client-dev")`, `sign_in(1)`을 수행해 `player_session_view` 세션을 실제로 생성했다.
- `web-client/.env.local`에 local token과 `VITE_CONNECT_ON_BOOT=true`를 넣고 별도 dev server(`127.0.0.1:5180`)에서 연결 모드 부팅을 검증했다.
- `AuthoritativeStore`는 `building_def`, `biome_gen_def`, `resource_gen_def`, `region_id` 등을 primary key 후보에 추가해 `BigInt` row가 `JSON.stringify` fallback으로 떨어지지 않게 수정했다.
- `SubscriptionCoordinator`의 `request_chunks_for_aoi` reducer 호출은 generated schema에 맞는 object payload(`regionId: BigInt(...)`, `dimensionId`, chunk bounds)로 바꿔 AOI 요청 경고를 제거했다.
- live 연결 검증에서 브라우저 DOM 텍스트와 서버 SQL 기준 `player_session_view`, `terrain_chunk_stream`, `terrain_chunk_payload`, `transform_state`, `resource_node`, `npc_state_stream` row 유입을 확인해 world loading이 실제로 동작함을 검증했다.

## 중단 시점 상태

- seed asset 스테이징은 한 차례 완료되어 `web-client/assets-src/seed/kenney/*` 아래에 실제 파일이 존재하는 상태까지 확인했다.
- `public/assets/seed`로의 재스테이징은 실행 중이었으나, turn 중단으로 최종 완료 여부는 다시 확인이 필요하다.
- Spacetime TypeScript bindings 생성은 `spacetime generate --lang typescript --module-path stitch-server/crates/game_server`로 재시도 중이었고, `cargo`/`game_server` 빌드 단계와 artifact lock 대기 로그를 확인했다.
- turn 중단 시점 기준으로 `web-client/src/module_bindings`에는 placeholder `index.ts`만 확인되었기 때문에, generated bindings 산출 여부는 다시 확인해야 한다.

## 재개 후 확인 결과

- `public/assets/seed/kenney/*` 재스테이징이 완료되어 Pixi preload 경로와 실제 정적 파일 경로가 일치하는 상태를 확인했다.
- `web-client/src/module_bindings/index.ts`와 다수의 table/reducer schema 파일이 실제 generated output으로 생성된 상태를 확인했다.
- generated bindings에는 `physics_state`, `player_movement_feedback_view`, `server_correction`, `submit_motion_intent`, `sync_client_frame`, `ack_server_correction`이 포함된다.
- `bun run build`가 성공해 현재 `web-client` 코드가 generated bindings 기준으로도 컴파일되는 것을 검증했다.
- `public/assets/seed/kenney/top-down-shooter/*`, `public/assets/seed/kenney/tiny-town/*`가 실제로 존재하는 것을 다시 확인했다.
- `bunfig.toml`의 hoisted linker 적용 후에도 `bun run build`가 계속 성공하는 것을 검증했다.
- `bunx tsc --noEmit`와 `bun run build`를 metadata 기반 renderer 수정 이후에도 다시 통과시켰다.
- `agent-browser` 세션에서 `canvas` 존재와 페이지 에러 부재를 다시 확인했고, dev console에는 Vite 연결 로그와 WebGL 성능 경고만 남았다.
- pointer targeting/claim overlay 추가 이후에도 `bunx tsc --noEmit`, `bun run build`를 다시 통과시켰다.
- `agent-browser` 세션에서 `pointer target` 버튼 노출을 확인했고, 현재 `connectOnBoot=false`라 `session pending` 상태인 탓에 실제 session anchor 기반 picking round-trip은 아직 검증하지 못했다.
- `spacetime sql --server 127.0.0.1:3000 stitch-server "SELECT COUNT(*) AS count FROM player_session_view"` 결과 1건으로 live session 생성이 확인됐다.
- `spacetime sql --server 127.0.0.1:3000 stitch-server "SELECT COUNT(*) AS count FROM terrain_chunk_stream WHERE region_id = 1 AND dimension_id = 1"` 결과 49건으로 월드 terrain stream이 준비된 것을 확인했다.
- 연결 모드 dev server(`127.0.0.1:5180`)에서 `agent-browser` DOM 텍스트 기준 `player_session_view=1`, `terrain_chunk_stream=49`, `terrain_chunk_payload=49`, `transform_state=1`, `resource_node=1`, `npc_state_stream=1` 유입을 확인했다.
- live 검증 스크린샷은 `/run/user/1000/agent-browser/tmp/screenshots/screenshot-2026-03-11T18-18-32-851Z-zhiqq1.png`에 저장했다.

## 바로 다음 할 일

1. inventory move를 split stack, cross-container, 오류 사유 HUD까지 확장하고 live reducer round-trip을 서버에서 검증한다.
2. building preview pointer picking을 live session 환경에서 검증하고, click 시 auto-validate/auto-place UX 여부를 정한다.
3. biome/resource/building def만으로 표현되지 않는 texture 선택 규칙을 전용 render metadata 계층으로 승격해 heuristic fallback을 제거한다.
