# 2026-02-16 Worklog (NPC Spawn/Render/Movement Hotfix)

## 1) 작업 범위
- 요청 사항:
  - 월드에 다양한 NPC 등장
  - 웹 클라이언트에서 NPC가 1마리만 보이는 현상 해결
  - NPC가 즉각 움직이도록 조정
- 주요 대상:
  - `stitch-server/crates/game_server/src/agents/mod.rs`
  - `web-client/src/render/world-streaming.ts`

## 2) 이슈 요약
- 서버 DB 기준 `npc_state`는 다수(22개) 존재했으나, 클라이언트에서 체감상 1마리만 보이는 상태가 발생.
- NPC 이동 주기가 300초(5분)로 길어 정지처럼 보이는 문제 존재.

## 3) 수행 내용
- [x] NPC 기본 시드 확장/보강
  - 시드 NPC를 22개로 유지하고, `start_world_agents` 실행 시 누락뿐 아니라 기존 시드 NPC도 동기화되도록 정리.
  - 퀘스트 시작 NPC `9001`, `9002` 포함 유지.

- [x] NPC 배치 좌표 재조정
  - 시작 지점 주변에 NPC가 즉시 보이도록 좌표를 근거리로 재배치.

- [x] 클라이언트 NPC 렌더 경로 안정화
  - `world-streaming`에서 NPC는 glTF 대신 instanced fallback geometry 경로로 렌더하도록 적용.
  - `three-stdlib` `SkeletonUtils` clone 경로도 반영된 상태 확인.

- [x] NPC 즉시 이동 설정
  - `NPC_AI_INTERVAL_SECS`를 `300 -> 2`로 축소.
  - `ensure_default_agent_timers`에서 기존 `npc_ai_loop_timer`도 interval을 강제 갱신하도록 수정.
  - `start_world_agents` 시 기존 `npc_action_schedule`를 업데이트하며,
    - `wander(1)`, `patrol(2)`는 `next_action_at=now_us`로 즉시 실행
    - `idle(0)`, `fixed(3)`는 지연 유지

## 4) 적용 명령 로그 (핵심)
- 서버 배포/적용:
  - `spacetime build`
  - `spacetime publish --server 127.0.0.1:3000 --yes stitch-server`
  - `spacetime call --server 127.0.0.1:3000 stitch-server start_world_agents`

- 웹 검증:
  - `agent-browser open http://127.0.0.1:5173`
  - 패널 텍스트에서 `npc=22` 확인
  - 스크린샷 캡처로 다수 NPC 표시 확인

## 5) 검증 결과
- DB 검증:
  - `SELECT COUNT(*) FROM npc_state` 결과: `22`
  - `npc_ai_loop_timer.scheduled_at` 결과: `+2.000000` interval
- 이동 검증:
  - 샘플 NPC(`10001`) 좌표가 약 4초 간격 조회에서 변경됨
    - 예: `(2, -1) -> (8, 3)`
- 클라이언트 상태:
  - 패널 요약 `npc=22` 노출
  - 화면에서 다수 NPC 렌더 확인

## 6) 현재 작업 트리 상태
- 변경 파일:
  - `stitch-server/crates/game_server/src/agents/mod.rs`
- 미추적 파일:
  - `prompts/20260216.txt`
  - `prompts/20260216_worklog.txt`

## 7) 후속 메모
- `idle(0)`, `fixed(3)` NPC는 의도적으로 정지 동작이 정상.
- 개발 환경에서는 2초 주기가 적합하나, 운영/부하 기준으로는 별도 값(예: 5~30초) 재조정 검토 필요.

---

# 2026-02-16 Worklog (Player Asset/Animation/SFX + WSL Google Human Login)

## 1) 작업 범위
- 요청 사항:
  - web client player asset 고도화
  - 방향 불일치/비정상 튐 개선
  - 이동 SFX 문제(발소리 대신 물소리 체감) 원인 추적
  - agent-browser + Google 로그인 차단 우회(사람 개입)
- 주요 대상:
  - `web-client/public/assets/manifest.json`
  - `web-client/src/render/asset-mapping.ts`
  - `web-client/src/render/world-streaming.ts`
  - `assetdirectory/README.md`
  - `assetdirectory/scripts/*`
  - `.agents/skills/wsl-google-human-login/*`
  - `.agents/skills/agent-browser/SKILL.md`
  - `.agents/skills/agent-browser/references/authentication.md`

## 2) 이슈 요약
- Google OAuth가 자동화 브라우저를 차단:
  - `This browser or app may not be secure`
- 임시 8-way alias에서 `wheelchair-move-*` 클립 사용으로 좌/우/후진이 튀어 보이는 증상 존재.
- 이동 SFX 체감 이슈:
  - 기존 footstep 매핑 중 긴/루프성 오디오가 발소리로 들리지 않는 문제 제기.

## 3) 수행 내용
- [x] 캐릭터 에셋/매핑 정리
  - 임시 플레이어 모델을 Kenney `character_gamer.glb`로 적용.
  - `manifest.json`에 캐릭터별 애니메이션 alias 키 유지:
    - `idle`, `walk_*`, `run_*`

- [x] 방향 블렌드 로직 적용 상태 확인
  - `world-streaming.ts`에서 월드 이동 벡터를 모델 로컬축으로 변환해
    `forward/back/left/right` 가중치 블렌딩하는 구조 확인.
  - footstep 트리거는 이동 거리 누적 기반 one-shot 재생 유지.

- [x] SFX 매핑 문서화 보강
  - `web-client/README.md`에 `SFX Mapping Notes` 정리.
  - footstep 매핑 소스 경로를 명시하고, 물/루프 계열 음원 혼입 방지 근거 기록.
  - `assetdirectory/audio/normalized/audio_index.csv` 기준으로 water/slime/loop 계열 존재도 확인.

- [x] Human-in-the-loop 로그인 스크립트 추가
  - `assetdirectory/scripts/mixamo_human_login_and_save.sh`
  - `assetdirectory/scripts/mixamo_open_with_saved_state.sh`
  - `assetdirectory/README.md`에 실행 절차 추가.

- [x] WSL + Windows Chrome CDP 브리지 실험/정착
  - Windows Chrome을 `--remote-debugging-port=9222`로 구동.
  - WSL에서 직접 9222 접근이 막혀 `portproxy(9223 -> 9222)` 경유로 연결.
  - `agent-browser`를 CDP websocket에 연결 후 Mixamo 로그인 세션 확인/저장 완료.

- [x] 신규 스킬 생성 (`$skill-creator` 요청 반영)
  - 스킬명: `wsl-google-human-login`
  - 파일:
    - `.agents/skills/wsl-google-human-login/SKILL.md`
    - `.agents/skills/wsl-google-human-login/scripts/start_windows_chrome_cdp.sh`
    - `.agents/skills/wsl-google-human-login/scripts/print_admin_portproxy_cmds.sh`
    - `.agents/skills/wsl-google-human-login/scripts/connect_agent_browser_cdp.sh`
  - 템플릿 예제 파일 제거.

- [x] 기존 `agent-browser` 스킬 문서와 연결
  - `.agents/skills/agent-browser/SKILL.md`에 WSL Google 차단 대응 링크 추가.
  - `.agents/skills/agent-browser/references/authentication.md` OAuth 섹션에 동일 링크 추가.

## 4) 적용 명령 로그 (핵심)
- Mixamo/Google 자동화 시도:
  - `agent-browser open https://www.mixamo.com/#/`
  - Adobe 로그인 → Google 버튼 진입
  - 결과: `accounts.google.com/.../signin/rejected`
- WSL 브리지:
  - Windows에서 Chrome CDP 실행(`9222`)
  - Admin PowerShell:
    - `netsh interface portproxy add v4tov4 ... listenport=9223 ... connectport=9222`
    - `netsh advfirewall firewall add rule ... localport=9223`
  - WSL 측 확인:
    - `curl http://<windows-host-ip>:9223/json/version`
    - `agent-browser connect <ws://...>`

## 5) 검증 결과
- Mixamo 로그인 상태 확인:
  - `snapshot -i`에서 `david` 계정 메뉴 확인(로그인 성공 상태).
- 인증 상태 저장:
  - `~/.cache/agent-browser/mixamo-auth-state.json` 생성.
- 저장 상태 단독 실행은 사이트 보안 정책/세션 특성상 항상 재현되지 않아
  CDP 직결 방식(Windows Chrome live session attach)을 기본 경로로 확정.

## 6) 산출물 요약
- 로그인 차단 대응 표준 경로 확보:
  - `agent-browser 단독 로그인` 실패 시
  - `Windows Chrome 수동 로그인 + WSL CDP attach`로 자동화 지속 가능.
- 재사용 가능한 운영 스크립트/스킬 문서 확보.

## 7) 후속 메모
- 애니메이션 품질 최종 개선을 위해 Mixamo 9클립(`idle`, `walk/run` f/b/l/r) 실제 다운로드/리타겟 반영 필요.
- 발소리 체감 이슈는 최종 선정 파일에 대해 청취 기반 QA(길이/어택/잔향) 추가 권장.
