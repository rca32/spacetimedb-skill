# 2026-02-12 Worklog (Phase 6 Building/Claim/Housing Implementation)

## 1) 작업 범위
- 대상: `WEBCLIENTDESIGN/11-implementation-order.md`의 Phase 6
- 기준 문서/코드:
  - `WEBCLIENTDESIGN/08-domain-building-claim-housing.md`
  - `stitch-server/crates/game_server/src/reducers/{building,claim,housing,id_allocator}/*`
  - `stitch-server/crates/game_server/src/tables/{building_state,claim_state,housing,id_lease_state}.rs`
  - `web-client/src/runtime/{build-claim-housing,ui,world,types}.ts`
  - `web-client/src/ui/{panels,hud}/index.ts`
  - `web-client/src/render/{materials,world-streaming}.ts`
- 목표:
  - Build/Claim/Housing 도메인 UI 액션 구현
  - ID 자동 할당(lease) 경로 구현
  - 월드/렌더러에 claim 시각화 반영
  - 수동 E2E 검증

## 2) 주요 구현 내용
- [x] 서버 ID allocator 경로 추가
  - `id_lease_state` public 테이블 추가
  - `building_next_id`, `claim_next_id`, `housing_next_id`, `dimension_network_next_id`, `dimension_desc_next_id` reducer 추가
  - nonce 검증(`request_nonce is required`) + `max + 1` lease 계산 + identity/kind 기준 upsert 적용

- [x] 클라이언트 Build/Claim/Housing runtime 구현
  - `build-claim-housing.ts`에서 도메인 subscription/read-model/actions 구현
  - ID lease 비동기 핸드셰이크(요청 nonce -> `id_lease_state` 응답 매칭 -> timeout 처리) 구현
  - `building_place/advance/deconstruct`, `claim_totem_place/expand`,
    `housing_create/enter/change_entrance/interior_mark_empty/housing_propagate_permissions/rent_set_whitelist` 연결
  - 구독 누락 복구를 위해 연결 중 `setSubscription`을 idempotent 재등록하도록 보강

- [x] 패널/HUD 연동 확장
  - 패널 탭 `B/C/H` 추가 및 입력 폼/액션 바인딩 구현
  - HUD 라인 `BUILD/CLAIM/HOUSE` 추가
  - 주거 선택값이 비어도 수동 ID 입력으로 동작하도록 fallback 반영

- [x] 네트워크 baseline 보강
  - `net.ts` baseline 구독에 `build-claim-housing-domain` 추가
  - 재연결/리로드 시 Phase 6 도메인 구독 복구 안정성 보강

- [x] ECS/렌더러 반영
  - `ClaimData` trait 추가
  - 월드 동기화에서 `claim_state`를 `ClaimData`로 적재
  - 렌더러에 claim ring instancing 추가(소유자/타인 색상 구분, dispose/pool 정리 포함)

## 3) 검증 결과
- 서버:
  - `cargo check -p game_server` 통과
  - `cargo test -p game_server` 통과(이전 실행 기준 15 pass)
- 클라이언트:
  - `bun run lint` 통과
  - `bun run build` 통과
  - `bun run typecheck` 실패(기존 이슈):
    - `src/runtime/sync-engine.test.ts(1,38): Cannot find module 'bun:test'...`

## 4) E2E 메모
- 세션 A 기준으로 Build/Claim/Housing 핵심 시나리오 동작 및 SQL 반영 확인
  - 건축 배치/완공
  - 클레임 생성/확장
  - 주거 생성
- 권한/화이트리스트 검증(세션 A owner, 세션 B non-owner)
  - A에서 `rent_set_whitelist(housing=1)`로 B identity 추가/제거를 반복
  - SQL `rent_state.white_list`로 반영 확인
  - B의 `housing_enter(housing=1)` 호출 전/후 `transform_state.updated_at` 비교로 권한 반영 확인
    - 비허용 상태: `updated_at` 변화 없음
    - 허용(화이트리스트 추가) 상태: `updated_at` 갱신됨

## 5) 잔여 이슈
- 리로드 이후 일부 세션에서 `build-claim-housing-domain`의 `subscription applied` 로그가 누락되고,
  패널의 build/claim/housing read-model count가 `0`으로 유지되는 케이스가 재현됨
- reducer 액션 자체(`rent_set_whitelist`, `housing_enter`)는 정상 dispatch/반영됨을 SQL로 확인
- 원인 후보: SDK subscription 콜백 미수신 또는 도메인 키 재활성화 타이밍
- 후속: net/runtime에 구독 상태 계측 라인(`active specs`, `last apply/error per key`) 추가 필요

## 6) 문서 상태 반영
- `WEBCLIENTDESIGN/11-implementation-order.md`
  - Phase 6 체크리스트 4개 항목 완료(`[x]`) 반영

## 7) Follow-up (요청 1,2)
- [x] 구독 상태 HUD 계측 추가
  - `NetRuntimeBridge.getSubscriptionDiagnostics()` 추가
  - `SubscriptionRegistry.snapshot()` 추가
  - HUD `SUBS` 라인에서 key별 `active/applied/error` 상태 노출

- [x] `bun:test` 타입 오류 해결
  - `src/types/bun-test.d.ts` 선언 파일 추가
  - `bun run typecheck` 통과 확인

## 8) Follow-up (구독 원인 추적/보정)
- [x] Build/Claim/Housing 구독을 단일 멀티쿼리에서 테이블별 키로 분리
  - `bch-building-def`, `bch-building-state`, `bch-claim-state`, `bch-housing-state`,
    `bch-dimension-network`, `bch-dimension-desc`
- [x] `id_lease_state` 미구독 시 자동 ID 발급이 timeout 되던 문제 보정
  - `building_next_id` / `claim_next_id` / `housing|dimension_*_next_id` timeout 시
    snapshot 기준 `max+1` 로컬 fallback ID를 사용해 reducer 디스패치 지속
- [x] 실검증
  - UI: `building_next_id fallback used ... -> building_place dispatched (building=2)` 확인
  - SQL: `building_state`에 `entity_id=2` 신규 row 생성 확인

## 9) Follow-up (rent_state decode crash 우회)
- [x] 원인 재현
  - 최소 Bun 스크립트에서 `SELECT * FROM rent_state` 구독 시 SDK decode crash 재현
    - `RangeError: Out of bounds access` (`readU256` 경로)
  - 대조군 `SELECT * FROM id_lease_state`는 정상 `applied`

- [x] 서버 projection 테이블 추가
  - `rent_whitelist_entry` public 테이블 추가 (`entry_key`, `housing_entity_id`, `identity`)
  - `housing_create`, `rent_set_whitelist`에서 `rent_state` 변경 후 projection 동기화
  - `sync_rent_whitelist_entries()` 헬퍼로 기존 엔트리 정리 + 재삽입

- [x] 클라이언트 소비 경로 전환
  - B/C/H 구독에서 `rent_state` 대신 `rent_whitelist_entry` 사용
  - read-model `collectRentStates()`를 projection row 그룹핑 방식으로 교체
  - HUD 별칭 `bch-rent-whitelist-entry -> bch:rent` 반영

- [x] 배포/검증
  - `spacetime publish --server 127.0.0.1:3000 stitch-server` 재배포 완료
  - 마이그레이션: `rent_whitelist_entry` 생성 확인
  - 브라우저 HUD: `SUBS ... bch:rent:on/a1/ok` 확인
  - 브라우저 콘솔/에러 로그에서 subscription/decode 오류 미재현
  - `cargo check -p game_server`, `bun run lint`, `bun run typecheck`, `bun run build`, `bun test` 통과


---

# 2026-02-13 Worklog (Phase 5.5 Asset Pipeline)

## 1) 작업 범위
- 대상: `WEBCLIENTDESIGN/11-implementation-order.md` Phase 5.5 (신규 추가)
- 기준 요청: "web-client에 게임 에셋관련 계획이 없는듯 한데.."
- 목표:
  - 에셋 디렉토리 구조 및 플레이스홀더 에셋 배치
  - `WEBCLIENTDESIGN/12-asset-strategy.md` 작성
  - `asset-loader.ts`, `asset-mapping.ts` 구현
  - 오디오(SFX/BGM) 포함

## 2) 주요 작업 내용

### 2.1 디렉토리 구조 생성
```
web-client/public/assets/
├── manifest.json
├── models/
│   ├── characters/    # player_placeholder.glb
│   ├── buildings/     # box.glb, box_textured.glb
│   ├── props/         # resource_{small,medium,large}.glb
│   └── effects/
├── textures/
│   ├── terrain/
│   ├── particles/
│   └── ui/
└── audio/
    ├── sfx/           # 8개
    └── music/         # 2개
```

### 2.2 플레이스홀더 에셋 다운로드
**모델 (Khronos glTF-Sample-Models)**
| 파일 | 소스 | 크기 |
|------|------|------|
| player_placeholder.glb | CesiumMan | 480KB |
| box.glb | Box | 1.7KB |
| box_textured.glb | BoxTextured | 6.4KB |
| resource_large.glb | Duck | 118KB |
| resource_medium.glb | WaterBottle | 8.6MB |
| resource_small.glb | Avocado | 8.0MB |

**오디오 SFX (Mixkit Free)**
| 파일 | 용도 | 크기 |
|------|------|------|
| ui_click.mp3 | UI 버튼 클릭 | 8.3KB |
| ui_success.mp3 | 성공 알림 | 90KB |
| ui_error.mp3 | 에러 알림 | 63KB |
| footstep_01.mp3 | 발소리 1 | 42KB |
| footstep_02.mp3 | 발소리 2 | 939KB |
| attack_swing.mp3 | 공격 스윙 | 19KB |
| attack_hit.mp3 | 공격 타격 | 29KB |
| gather_harvest.mp3 | 채집/수확 | 889KB |

**오디오 BGM (Kevin MacLeod / Incompetech, CC-BY 3.0)**
| 파일 | 원곡명 | 크기 |
|------|--------|------|
| ambient_sneaky.mp3 | Sneaky Snitch | 5.3MB |
| gameplay_upbeat.mp3 | Spazzmatica Polka | 3.7MB |

### 2.3 구현 파일
- [x] `public/assets/manifest.json` - 에셋 매니페스트 (preloadPriority: critical/high/normal/lazy)
- [x] `src/render/asset-mapping.ts` - 타입 정의, 매핑 함수, SFX/MUSIC 상수
- [x] `src/render/asset-loader.ts` - GLTFLoader, AudioBuffer 로딩, 캐싱, playSfx()/playMusic() API
- [x] `WEBCLIENTDESIGN/12-asset-strategy.md` - 에셋 전략 문서
- [x] `WEBCLIENTDESIGN/11-implementation-order.md` - Phase 5.5 체크리스트 추가
- [x] `WEBCLIENTDESIGN/00-master.md` - Deliverables에 12-asset-strategy.md 추가
- [x] `three-stdlib@2.36.1` 의존성 추가

## 3) 작업 중 확인한 핵심 이슈
- Kenney 에셋 팩 다운로드 URL 404
  - 대안: Khronos glTF-Sample-Models 사용
- 일부 오디오 파일 용량 과대 (peaceful_ambient.mp3 86MB)
  - 조치: 해당 파일 삭제, 2개 BGM만 유지
- `peaceful_ambient.mp3` 다운로드 실패
  - 조치: manifest.json에서 제거, 2개 BGM으로 구성

## 4) 변경 파일
- `web-client/public/assets/` (신규 디렉토리 및 파일 16개)
- `web-client/public/assets/manifest.json` (신규)
- `web-client/src/render/asset-loader.ts` (신규)
- `web-client/src/render/asset-mapping.ts` (신규)
- `web-client/package.json` (three-stdlib 추가)
- `WEBCLIENTDESIGN/12-asset-strategy.md` (신규)
- `WEBCLIENTDESIGN/11-implementation-order.md` (Phase 5.5 추가)
- `WEBCLIENTDESIGN/00-master.md` (Deliverables 업데이트)
- `prompts/20260211_worklog.md` (본 업데이트)

## 5) 검증 결과
- 정적 검사:
  - `cd web-client && bun run typecheck` - asset 파일 관련 에러 없음
- 에셋 확인:
  - 총 16개 파일, 29MB 다운로드 완료
  - manifest.json 형식 유효

## 6) 후속 작업 (다른 AI 에이전트용)
- [x] `LoadingAssets` 상태에서 `AssetLoader.loadCriticalAssets()` 통합
- [x] `WorldStreamingRenderer`에 glTF 모델 매핑
- [ ] 캐릭터/건물/프롭 모델을 플레이스홀더에서 프로덕션으로 교체
- [ ] 오디오 재생 트리거 연결 (이동/공격/UI 클릭)
- [ ] BGM 루프/페이드 인아웃 구현

## 7) 라이선스 참고
- 모델: Khronos glTF-Sample-Models (CC0-like)
- SFX: Mixkit (Free, 출처 표기 불필요)
- BGM: Kevin MacLeod / Incompetech (CC-BY 3.0, 출처 표기 필수)
  - 사용 시: "Music: https://incompetech.com/"

---

## 8) Follow-up (Stock Assets 확보) - 2026-02-13

### 작업 내용
- [x] `stock/` 디렉토리 구조 생성
  - models/{characters,buildings,props,animals,effects,weapons,vehicles,vegetation}
  - textures/{terrain,particles,ui,environment}
  - audio/{sfx,music,ambient}
- [x] Khronos glTF-Sample-Assets에서 모델 다운로드 (13개)
- [x] Mixkit에서 추가 SFX 다운로드 (6개)
- [x] `stock/index.json` 인덱스 파일 생성
- [x] `WEBCLIENTDESIGN/12-asset-strategy.md`에 Stock Assets 섹션(4절) 추가

### Stock 현황 (37MB, 21개)
| 카테고리 | 파일 수 | 크기 |
|----------|--------|------|
| models/characters | 3 | 3.7M |
| models/buildings | 3 | 21.4M |
| models/props | 5 | 8.5M |
| models/animals | 1 | 160K |
| models/effects | 1 | 2.9M |
| audio/sfx | 6 | 603K |
| textures | 1 | 136K |

### 추가 에셋 소스 (향후 다운로드용)
- Kenney (CC0): kenney.nl/assets
- Kaykit (무료/유료): kaylousberg.itch.io
- Quaternius (CC0): quaternius.com
- Poly Pizza (CC0/CC-BY): poly.pizza
- Mixkit (무료): mixkit.co/free-sound-effects/
- Kevin MacLeod (CC-BY 3.0): incompetech.com/music/

---

## 9) Follow-up (Phase 5.5 LoadingAssets 통합) - 2026-02-12

### 작업 내용
- [x] `web-client/src/app/bootstrap.ts`에 `LoadingAssets` 단계 실동작 통합
  - `Boot -> LoadingAssets -> Connecting` 전이를 고정
  - `LoadingAssets` 동안 `AssetLoader.loadCriticalAssets()` 실행
- [x] 로딩 실패 fallback 경로 추가
  - manifest/critical 에셋 로딩 실패 시 경고 로그만 남기고 연결 단계 계속 진행
  - 월드 렌더는 기존 프리미티브 파이프라인을 그대로 사용
- [x] shutdown 시 `AssetLoader.dispose()` 호출 추가
  - 모델/텍스처/오디오 캐시 해제 경로 보강
- [x] `WEBCLIENTDESIGN/11-implementation-order.md` Phase 5.5 체크리스트 상태 갱신
  - `three-stdlib` 의존성
  - `LoadingAssets` 통합 항목 완료 처리

### 변경 파일
- `web-client/src/app/bootstrap.ts`
- `WEBCLIENTDESIGN/11-implementation-order.md`
- `prompts/20260212_worklog.md`

---

## 10) Follow-up (WorldStreamingRenderer glTF 매핑 + 생성물 정리) - 2026-02-12

### 작업 내용
- [x] 월드 렌더러 glTF 매핑 통합
  - `web-client/src/render/world-streaming.ts`에서 manifest 기반 모델 경로 매핑 추가
  - 플레이어/원격플레이어/NPC/건물/자원노드를 glTF 우선 렌더링
  - 모델 미로딩/실패 시 기존 InstancedMesh 프리미티브 fallback 유지
  - 런타임 중 lazy model load 큐(`AssetLoader.loadModel`)로 비차단 로드
- [x] 월드 동기화 데이터 보강
  - `web-client/src/core/traits/index.ts`의 `BuildingData`에 `requiredItemDefId` 추가
  - `web-client/src/runtime/world.ts`에서 `building_state.required_item_def_id`를 ECS trait로 적재
- [x] 생성물 정리(요청 2)
  - `dist/`와 `.vite/deps` 변경은 정리 대상으로 분리
  - 최종 상태에서 소스/문서 변경만 남기도록 정리

### 변경 파일
- `web-client/src/render/world-streaming.ts`
- `web-client/src/runtime/world.ts`
- `web-client/src/core/traits/index.ts`
- `WEBCLIENTDESIGN/11-implementation-order.md`
- `prompts/20260212_worklog.md`
