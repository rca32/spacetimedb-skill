# 2026-02-18 Worklog (이동 후 정지 시 위치 점프 복귀 증상 수정)

## 1) 작업 범위
- 요청 사항:
  - 2026-02-17 이후 클라이언트에서 조금 이동 후 멈추면 이전 위치로 점프 복귀하는 증상 원인 분석/수정
- 주요 대상:
  - `stitch-server/crates/game_server/src/reducers/player/move_player.rs`
  - `stitch-server/crates/game_server/src/agents/mod.rs`
  - `web-client/src/runtime/sync-engine.ts` (원인 분석 대상)

## 2) 증상 요약
- 클라이언트 이동 직후 또는 정지 시점에 위치가 과거 authoritative 위치로 순간 복귀(jump)하는 현상 발생.
- HUD/피드백 기준으로 일부 세션에서 `missing_session` 이동 거절이 반복되는 패턴 확인.

## 3) 원인 분석
- 서버 `move_to` 리듀서가 정상 이동 처리 시 `session_state.last_active_at`를 갱신하지 않음.
  - 결과: 실제로 플레이 중이어도 세션 cleanup 타이머 대상이 될 수 있음.
- `session_cleanup_agent_loop`가 `session_state`만 삭제하고 `player_session_view` 정합을 즉시 맞추지 않음.
  - 결과: 클라이언트는 stale `player_session_view`를 보고 계속 이동 요청을 보냄.
  - 서버는 `missing_session`으로 거절하고, 클라이언트 prediction/reconcile에서 rollback이 발생해 점프처럼 보임.

## 4) 수행 내용
- [x] `move_to`에서 유효 세션/지역 검증 직후 `session_state.last_active_at`를 `ctx.timestamp`로 갱신.
- [x] `session_cleanup_agent_loop`에서 cleanup 후 `player_session_view` 전체 identity를 순회해 `sync_player_session_view` 재동기화.
  - 기존 누적된 stale view도 주기 실행으로 자동 정리되도록 조치.

## 5) 적용 명령 로그 (핵심)
- 서버 코드 검증:
  - `cd stitch-server && cargo check -p game_server`
  - `cd stitch-server && cargo test -p game_server`
- 배포/운영 반영:
  - `cd stitch-server/crates/game_server && spacetime build`
  - `spacetime publish --yes stitch-server`
  - `spacetime call stitch-server start_world_agents`
- 상태 검증:
  - `spacetime sql stitch-server "SELECT COUNT(*) AS count FROM player_session_view"`
  - `spacetime sql stitch-server "SELECT COUNT(*) AS count FROM session_state"`

## 6) 검증 결과
- `cargo check`: 통과
- `cargo test`: 통과 (`28 passed`)
- 운영 DB 정합성:
  - 수정/배포 전 샘플: `player_session_view=6`, `session_state=2` (stale 존재)
  - cleanup 주기 1회 경과 후: `player_session_view=2`, `session_state=2` (정합 회복)
- 사용자 재확인:
  - "사라졌음"으로 증상 해소 확인.

## 7) 변경 파일 요약
- 수정:
  - `stitch-server/crates/game_server/src/reducers/player/move_player.rs`
  - `stitch-server/crates/game_server/src/agents/mod.rs`

## 8) 재발 방지 메모
- 세션 기반 리듀서(`move_to` 등)는 성공 경로에서 `last_active_at` 갱신을 보장해야 함.
- `session_state`를 정리하는 루프는 projection view(`player_session_view`)도 같은 트랜잭션 흐름에서 정합 유지 필요.

---

# 2026-02-18 Worklog (Hex-grid coordinate system 통합 점검 + 1/2/3 마무리)

## 1) 작업 범위
- 요청 사항:
  - 아래 3가지 항목을 모두 마무리
    1. `transform/session/npc/claim` dimension 지원 정합화
    2. worldgen reducer dimension 인자화
    3. dimension-switch reducer + 클라이언트 계약 연결
- 주요 대상:
  - `stitch-server/crates/game_server/src/auth/mod.rs`
  - `stitch-server/crates/game_server/src/reducers/worldgen/mod.rs`
  - `web-client/src/net/aoi.ts`
  - `web-client/src/runtime/world.ts`
  - `web-client/src/runtime/build-claim-housing.ts`
  - `web-client/src/ui/panels/index.ts`
  - `web-client/src/runtime/world-systems/common.ts`
  - `web-client/src/runtime/types.ts`
  - `web-client/src/module_bindings/*` (재생성)

## 2) 수행 내용
- [x] `set_active_dimension` 검증 확장
  - 기존 `dimension_desc` 기반 검증에 더해, worldgen으로 생성된 차원(`terrain_chunk`)도 전환 가능하도록 확장.
  - 구현: `auth/mod.rs`에서 세션 region 기준으로 `terrain_chunk.dimension_id` 존재 여부 확인.

- [x] worldgen 리듀서 dimension 인자화
  - 신규 리듀서 추가:
    - `generate_world_in_dimension`
    - `generate_world_from_params_in_dimension`
    - `regenerate_chunks_in_dimension`
  - 기존 리듀서(`generate_world`, `generate_world_from_params`, `regenerate_chunks`)는 기본 차원(1) wrapper로 유지해 하위 호환성 보장.

- [x] 클라이언트 dimension 계약 연결
  - 월드 AOI 쿼리에서 `transform_state`, `npc_state`, `claim_state`에 `dimension_id` 필터 적용.
  - `world.ts`에서 `playerSessionView.dimensionId`를 authoritative 현재 차원으로 사용.
  - Build/Claim/Housing 런타임에 `set_active_dimension` 액션 추가.
  - 패널(Housing)에 `Set Active Dimension` 버튼 추가.
  - 클레임/NPC/session row 타입에 `dimensionId` 반영 및 동기화 경로 정리.

- [x] 바인딩 재생성
  - 신규 리듀서/필드 반영을 위해 `web-client/src/module_bindings` 재생성.
  - 신규 생성 파일:
    - `generate_world_in_dimension_reducer.ts`
    - `generate_world_from_params_in_dimension_reducer.ts`
    - `regenerate_chunks_in_dimension_reducer.ts`
    - `set_active_dimension_reducer.ts`

## 3) 적용 명령 로그 (핵심)
- 서버:
  - `cd stitch-server && cargo check -p game_server`
  - `cd stitch-server && cargo test -p game_server`
- 클라이언트:
  - `cd web-client && bun run spacetime:generate`
  - `cd web-client && bun run typecheck`
  - `cd web-client && bun run lint`
  - `cd web-client && bun run build`
- 스모크:
  - `stitch-server/scripts/full_smoke_gate.sh --server 127.0.0.1:3000 --db stitch-server`
- dimension 수동 검증:
  - `spacetime call ... generate_world_in_dimension 1 2 4242 3 3 true`
  - `spacetime sql ... "SELECT COUNT(*) AS count FROM terrain_chunk WHERE region_id = 1 AND dimension_id = 2"`
  - `spacetime call ... set_active_dimension 2`
  - `spacetime call ... request_path_in_dimension 1 2 0 0 3 3 512`
  - `spacetime sql ... "SELECT COUNT(*) AS count FROM path_result WHERE region_id = 1 AND dimension_id = 2"`

## 4) 검증 결과
- 서버:
  - `cargo check`: 통과
  - `cargo test`: 통과 (`28 passed`)
- 클라이언트:
  - `spacetime:generate`: 통과
  - `typecheck`: 통과
  - `lint`: 통과
  - `build`: 통과
- 통합 스모크:
  - `full_smoke_gate`: PASS
- dimension 시나리오:
  - `terrain_chunk(region=1, dimension=2) = 9` 확인
  - `set_active_dimension 2` 성공
  - `path_result(region=1, dimension=2) = 1` 확인
  - `set_active_dimension 999`는 의도대로 `target dimension does not exist` 에러 반환

## 5) 변경 파일 요약
- 서버 수정:
  - `stitch-server/crates/game_server/src/auth/mod.rs`
  - `stitch-server/crates/game_server/src/reducers/worldgen/mod.rs`
- 클라이언트 수정:
  - `web-client/src/net/aoi.ts`
  - `web-client/src/runtime/world.ts`
  - `web-client/src/runtime/build-claim-housing.ts`
  - `web-client/src/ui/panels/index.ts`
  - `web-client/src/runtime/world-systems/common.ts`
  - `web-client/src/runtime/world-systems/claim-sync-system.ts`
  - `web-client/src/runtime/social-npc-quest.ts`
  - `web-client/src/runtime/combat.ts`
  - `web-client/src/runtime/inventory-trade.ts`
  - `web-client/src/runtime/sync-engine.ts`
  - `web-client/src/runtime/ui.ts`
  - `web-client/src/runtime/types.ts`
  - `web-client/src/core/traits/index.ts`
- 바인딩 재생성/추가:
  - `web-client/src/module_bindings/*` (session/transform/npc/claim 반영 + 신규 in_dimension reducer 바인딩)

---

# 2026-02-18 Worklog (Hex-grid 잔여 개발 4/5 완료: 자동화 테스트 + Dimension UX)

## 1) 작업 범위
- 요청 사항:
  - 이전 점검에서 남은 항목을 전체 완료
    1. 자동화 테스트 보강 (`set_active_dimension`, in-dimension worldgen reducers)
    2. UI dimension 전환 UX 개선(드롭다운 기반)
- 주요 대상:
  - `stitch-server/scripts/full_smoke_gate.sh`
  - `web-client/src/ui/panels/index.ts`
  - `web-client/src/module_bindings/*` (스키마 재생성 반영)

## 2) 수행 내용
- [x] 통합 스모크 스크립트에 dimension 시나리오 자동 검증 추가
  - `generate_world_in_dimension`
  - `generate_world_from_params_in_dimension`
  - `regenerate_chunks_in_dimension`
  - `set_active_dimension` 성공 경로
  - `set_active_dimension 999999` 실패 경로(에러 문자열 확인)
  - `request_path_in_dimension` 결과 검증
- [x] 스모크 안정성 보정
  - 기존 `req_probe -> terrain_blocked`는 월드/차원 상태에 따라 비결정적이어서 제거.
  - 이동 피드백 핵심 검증(`ok`, `distance_exceeded`)은 유지.
- [x] Housing 패널 dimension picker UX 개선
  - `Dimension Picker` 드롭다운 추가.
  - 선택값 우선 + 수동 입력(`Dimension Id (manual)`) fallback 구조 적용.
  - known dimension 집합(`dimension_desc`, `claims`, `buildings`, 수동 입력)을 동적으로 옵션 동기화.
  - `set_active_dimension`, `create_housing` 액션 모두 picker 기반으로 입력 해석.
- [x] 바인딩 재생성
  - combat/trade dimension 필드 추가 반영을 위해 TS module bindings 재생성.

## 3) 적용 명령 로그 (핵심)
- 서버:
  - `cd stitch-server && cargo check -p game_server`
  - `cd stitch-server && cargo test -p game_server`
  - `stitch-server/scripts/full_smoke_gate.sh --server 127.0.0.1:3000 --db stitch-server`
- 클라이언트:
  - `cd web-client && bun run spacetime:generate`
  - `cd web-client && bun run typecheck`
  - `cd web-client && bun run lint`
  - `cd web-client && bun run build`
- UI 스모크(agent-browser):
  - `bun run dev --host 127.0.0.1 --port 5174`
  - `agent-browser open http://127.0.0.1:5174`
  - `agent-browser click @e6` (Housing 탭)
  - `agent-browser snapshot -i` (Dimension Picker/Custom 입력 확인)
  - `agent-browser screenshot --full /tmp/stitch-housing-dimension-picker.png`

## 4) 검증 결과
- 서버:
  - `cargo check`: 통과
  - `cargo test`: 통과 (`28 passed`)
  - `full_smoke_gate`: PASS
    - `terrain_chunk(region=1, dimension=2) >= 1` 확인
    - `session_state(region=1, dimension=2) >= 1` 확인
    - `path_result(region=1, dimension=2)` 성공 확인
    - invalid dimension 전환 거절 메시지 확인
- 클라이언트:
  - `spacetime:generate`: 통과
  - `typecheck`: 통과
  - `lint`: 통과
  - `build`: 통과
- 브라우저:
  - Housing 탭에서 `Dimension Picker` + `Custom (manual input)` 옵션 노출 확인.

## 5) 변경 파일 요약
- 서버:
  - `stitch-server/scripts/full_smoke_gate.sh`
- 클라이언트:
  - `web-client/src/ui/panels/index.ts`
  - `web-client/src/module_bindings/attack_outcome_table.ts`
  - `web-client/src/module_bindings/attack_outcome_type.ts`
  - `web-client/src/module_bindings/attack_schedule_state_table.ts`
  - `web-client/src/module_bindings/attack_scheduled_type.ts`
  - `web-client/src/module_bindings/combat_state_table.ts`
  - `web-client/src/module_bindings/combat_state_type.ts`
  - `web-client/src/module_bindings/trade_session_table.ts`
  - `web-client/src/module_bindings/trade_session_type.ts`

---

# 2026-02-18 Worklog (추가 기록 요청 반영)

## 1) 요청
- 사용자 재요청:
  - `prompts/20260218_worklog.md`에 작업 로그 기록 재확인/반영

## 2) 처리 내용
- 본 문서에 이미 반영된 `Hex-grid 잔여 개발 4/5 완료` 섹션 존재를 확인.
- 재요청 이력 추적을 위해 본 확인 섹션을 추가.

## 3) 확인 시각
- `2026-02-18 14:07:17 KST`

---

## 7) 후속 메모 (초기)
- 본 섹션은 1차 작업 시점의 우선순위 메모이며, 8~10절에서 실제 완료 상태를 갱신했다.

---

## 8) 미완료 WP 추가 완료 (2026-02-18)
- [x] `WP-04` 완료: NPC 주문 라이프사이클 구현
  - 신규 테이블:
    - `npc_trade_order_def`
    - `npc_trade_order_state`
  - `npc_ai_agent_loop` 내 population reconcile 시 주문 상태 생성/갱신/삭제 반영

- [x] `WP-05` 완료: 권한/운영 게이트 강화
  - `feature_flags`에 `npc_ai_enabled` 기본 플래그 보장
  - `npc_ai_agent_loop`에 플래그 실행 게이트 반영
  - admin reducer:
    - `set_npc_ai_enabled`

- [x] LLM 최소 경로 완료
  - 신규 reducer:
    - `npc_dialogue_request`
    - `npc_action_resolve`
  - 연계 테이블:
    - `npc_action_request`, `npc_action_result`
    - `npc_conversation_session`, `npc_conversation_turn`
    - `npc_response_cache`, `npc_cost_metrics`, `npc_policy_violation`

## 9) 추가 검증 결과
- 서버:
  - `cargo check -p game_server`: 통과
  - `cargo test -p game_server`: 통과 (`29 passed`)
  - `cargo test -p data_loader`: 통과 (`4 passed`)
- 웹:
  - `bun run spacetime:generate`: 통과
  - `bun run typecheck`: 통과
  - `bun run build`: 통과
- 스모크:
  - `stitch-server/scripts/npc_ai_smoke_gate.sh`: `PASS`
  - `stitch-server/scripts/npc_ai_smoke_gate.sh --skip-publish`: `PASS`
  - 확인 항목:
    - `npc_trade_order_def enabled` 카운트 통과
    - `npc_trade_order_state` 카운트 통과
    - traveling NPC 위치 변화 감지 통과

## 10) 남은 WP 완료 (2026-02-18 2차)
- [x] `WP-06` 이동 목적지 선택 고도화
  - 최근 방문 회피 + 거리 기반 `top-k` 랜덤 선택
  - 후보 부재 시 `compute_npc_destination` fallback 유지
- [x] `WP-07` 시간 분포 개선
  - `npc_next_delay`를 타입별 `min/max` 난수 분산으로 보강
  - `schedule_kind`/시간 버킷 기반 지터 반영
- [x] `WP-08` AOI 기반 NPC 구독 축소
  - 서버 `npc_state_stream_query` 추가
  - 웹 AOI 쿼리에서 `npc_state`를 bounds 필터로 제한
- [x] `WP-09` 고아/깨진 앵커 정리 루프 고도화
  - 무효 앵커 NPC 정리 + stale schedule/path/order 정리 pass 추가

## 11) 현재 잔여 WP
- `NPCDESIGN` 기준 `WP-01`~`WP-10` 모두 완료.

## 12) 작업 로그 기록 요청 반영 (2026-02-18 3차)
- 사용자 요청: `남은 WP작업 해죠` + `작업 로그기록 해죠: prompts/20260218_worklog.md`
- 반영 사항:
  - `WP-06`~`WP-09` 완료 상태를 문서/코드/검증 결과와 함께 최종 기록
  - `NPCDESIGN/20260218_npc-ai-gap-analysis-and-llm-extension.md`에 잔여 WP 없음 상태 동기화
- 최종 검증 스냅샷:
  - `cargo check -p game_server`: 통과
  - `cargo test -p game_server`: 통과 (`29 passed`)
  - `bun run typecheck` / `bun run build`: 통과
  - `stitch-server/scripts/npc_ai_smoke_gate.sh` / `--skip-publish`: 모두 `PASS`
