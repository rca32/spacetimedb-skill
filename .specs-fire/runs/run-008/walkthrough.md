---
run: run-008
work_item: establish-full-cli-integration-regression-suite
intent: stitch-full-game-server-development
generated: 2026-02-08T00:25:58Z
mode: confirm
---

# Implementation Walkthrough: 전체 기능 CLI 통합 회귀 시나리오 확립

## Summary
핵심 도메인 루프를 재실행 가능한 CLI 스크립트로 표준화했다. `scripts/cli_regression_suite.sh`는 단일 실행과 반복 실행을 모두 지원하며, 실패 진단 SQL/점검 순서를 `docs/cli-regression-suite.md`에 정리했다. 또한 README에 회귀 스위트 진입점을 추가해 운영자가 즉시 실행할 수 있도록 했다.

## Structure Overview
회귀 자동화는 서버 코드 변경 없이 운영 계층에서 구성했다. 스크립트는 reducer 호출과 SQL 점검을 단계별로 수행하며, 예상 실패(전투 가드레일)도 포함해 음성 경로를 검증한다. 문서는 자동 스크립트 범위와 2-identity가 필요한 분할 시나리오를 분리해, AI tester/수동 테스트가 동일 기준으로 동작하도록 맞췄다.

## Files Changed

### Created

| File | Purpose |
|------|---------|
| `stitch-server/scripts/cli_regression_suite.sh` | end-to-end CLI 회귀 실행기 (`--repeat`, `--dry-run`, SQL 진단 포함) |
| `stitch-server/docs/cli-regression-suite.md` | 실행 절차, 합격 기준, 실패 진단 SQL/순서 문서 |

### Modified

| File | Changes |
|------|---------|
| `stitch-server/README.md` | CLI 회귀 스위트 실행 방법/문서 링크 추가 |

## Key Implementation Details

### 1. Repeatable regression harness
스크립트는 iteration 단위(run tag, deterministic id)로 실행해 동일 시퀀스를 반복 검증한다.

### 2. Domain flow coverage in one path
자동 시나리오에 auth/movement/inventory/building/claim/combat guardrail/npc/quest/market을 포함해 핵심 루프를 한 번에 점검한다.

### 3. Failure diagnosis playbook
문서에 점검 순서와 SQL 체크리스트를 명시해 실패 시 재현/triage 절차를 고정했다.

## Decisions Made

| Decision | Choice | Rationale |
|----------|--------|-----------|
| 구현 위치 | 서버 코드가 아닌 `scripts/` + `docs/` | 회귀 시나리오는 운영 실행성/문서화가 우선이기 때문 |
| 전투/거래 성공 경로 | 2-identity 분할 시나리오로 분리 | 단일 자동 identity에서 직접 거래/공격 성공 검증 한계가 있음 |
| 음성 테스트 포함 | `attack_scheduled` expected fail | 가드레일(실패 경로) 회귀도 합격 기준에 포함하기 위함 |

## Deviations from Plan
None.

## Dependencies Added

| Package | Why Needed |
|---------|------------|
| None | 쉘 스크립트/문서 변경만 수행 |

## How to Verify

1. **Prepare server + publish module**
```bash
spacetime start
cd /home/rca32/workspaces/spacetimedb-skill/stitch-server/crates/game_server
spacetime build
spacetime publish --server 127.0.0.1:3000 stitch-server
```
Expected: local DB publish succeeds.

2. **Run regression suite**
```bash
cd /home/rca32/workspaces/spacetimedb-skill/stitch-server
bash scripts/cli_regression_suite.sh --db stitch-server --server 127.0.0.1:3000 --repeat 1
```
Expected: script exits 0 and prints per-domain SQL checks.

3. **Repeatability check**
```bash
REPEAT=3 bash scripts/cli_regression_suite.sh --db stitch-server --server 127.0.0.1:3000
```
Expected: all iterations pass with same step sequence and criteria.

## Test Coverage
- Tests added: 0
- Coverage: not available
- Status: passing (script syntax + build checks)

## Developer Notes
회귀 스크립트는 서버가 기동 중이 아니면 첫 SQL preflight에서 즉시 실패한다. CI/로컬 자동화 시 `spacetime start`와 publish 완료를 선행해야 한다.

---
*Generated by specs.md - fabriqa.ai FIRE Flow Run run-008*
