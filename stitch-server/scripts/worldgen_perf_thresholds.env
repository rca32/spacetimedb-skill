# Worldgen performance gate thresholds (region=1, seed=1337, size=7x7)
# Updated: 2026-02-18
# Observed samples on dev machine:
# - 10 runs: avg_ms=624.30, p95_ms=1107, max_payload_bytes_estimate=401408
# - 5 runs:  avg_ms=862.60, p95_ms=1008, max_payload_bytes_estimate=401408
#
# Gate policy:
# - Use conservative local thresholds to avoid flaky failures under background load.
# - payload_bytes threshold stays tight because payload shape should be stable for fixed world size.

WORLDGEN_PERF_MAX_AVG_MS=1000
WORLDGEN_PERF_MAX_P95_MS=1500
WORLDGEN_PERF_MAX_PAYLOAD_BYTES=420000
