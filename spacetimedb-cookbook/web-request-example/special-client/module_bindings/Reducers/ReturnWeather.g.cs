// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void ReturnWeatherHandler(ReducerEventContext ctx, string response);
        public event ReturnWeatherHandler? OnReturnWeather;

        public void ReturnWeather(string response)
        {
            conn.InternalCallReducer(new Reducer.ReturnWeather(response), this.SetCallReducerFlags.ReturnWeatherFlags);
        }

        public bool InvokeReturnWeather(ReducerEventContext ctx, Reducer.ReturnWeather args)
        {
            if (OnReturnWeather == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnReturnWeather(
                ctx,
                args.Response
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class ReturnWeather : Reducer, IReducerArgs
        {
            [DataMember(Name = "response")]
            public string Response;

            public ReturnWeather(string Response)
            {
                this.Response = Response;
            }

            public ReturnWeather()
            {
                this.Response = "";
            }

            string IReducerArgs.ReducerName => "ReturnWeather";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags ReturnWeatherFlags;
        public void ReturnWeather(CallReducerFlags flags) => ReturnWeatherFlags = flags;
    }
}
