// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void RequestWeatherHandler(ReducerEventContext ctx, string latitude, string longitude);
        public event RequestWeatherHandler? OnRequestWeather;

        public void RequestWeather(string latitude, string longitude)
        {
            conn.InternalCallReducer(new Reducer.RequestWeather(latitude, longitude), this.SetCallReducerFlags.RequestWeatherFlags);
        }

        public bool InvokeRequestWeather(ReducerEventContext ctx, Reducer.RequestWeather args)
        {
            if (OnRequestWeather == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnRequestWeather(
                ctx,
                args.Latitude,
                args.Longitude
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class RequestWeather : Reducer, IReducerArgs
        {
            [DataMember(Name = "latitude")]
            public string Latitude;
            [DataMember(Name = "longitude")]
            public string Longitude;

            public RequestWeather(
                string Latitude,
                string Longitude
            )
            {
                this.Latitude = Latitude;
                this.Longitude = Longitude;
            }

            public RequestWeather()
            {
                this.Latitude = "";
                this.Longitude = "";
            }

            string IReducerArgs.ReducerName => "RequestWeather";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags RequestWeatherFlags;
        public void RequestWeather(CallReducerFlags flags) => RequestWeatherFlags = flags;
    }
}
