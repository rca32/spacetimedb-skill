# Orillusion Porting 작업 로그 (2026-02-19)

## 후속 문서 안내 (2026-02-21)
- 본 문서는 2026-02-19 기준 로그이다.
- 최신 반영(고스트 원인/해결, heightfield 기반 이동, 서버 지형 검증)은
  `IMPLEMENTDOC/logs/implementation/2026-02-21.md`를 우선 참조한다.

## 범위
- 대상 프로젝트:
  - `stitch-orillusion-client`
  - `stitch-server`
- 목표:
  - 렌더 크래시/구독 오류/입력 이슈를 제거하고 실제 월드(terrain/npc/resource/player)가 보이도록 안정화
  - hex 좌표 고려 경로를 클라이언트 구조에 명시적으로 반영

## 클라이언트 변경 사항

### 1) AOI/청크 크기 동기화
- `world_gen_params` 구독을 AOI 쿼리에 추가
- 런타임에서 `terrain_chunk_size`를 읽어 `activeChunkSize`로 동기화
- HUD에 chunk-size 표시 추가
- 반영 파일:
  - `stitch-orillusion-client/src/net/aoi.ts`
  - `stitch-orillusion-client/src/app/runtime.ts`

### 2) 카메라/마우스 입력 안정화
- `CameraAimComponent`에 외부 캔버스 주입 필드 추가
- 엔진 bootstrap의 실제 canvas를 pointer lock 대상으로 연결
- 반영 파일:
  - `stitch-orillusion-client/src/camera/camera-aim-component.ts`
  - `stitch-orillusion-client/src/engine/engine-bootstrap.ts`
  - `stitch-orillusion-client/src/app/runtime.ts`

### 3) 지형 렌더 크래시 방지 및 청크 스케일 정합
- 지형 재질 경로를 `UnLitMaterial` 기반으로 전환 (Lit pipeline 생성 실패 회피)
- payload 기반 chunk size 추론 + chunk world size 동기 반영
- terrain/npc/resource 좌표 배치 경로를 분리 정리
- 반영 파일:
  - `stitch-orillusion-client/src/world/stream-visualizer.ts`

### 4) hex 좌표 유틸 모듈 추가
- `hex/offset/world` 변환 유틸을 클라이언트에 독립 추가
- 반영 파일:
  - `stitch-orillusion-client/src/core/hex/hex-coords.ts`

## 서버 변경 사항

### 5) 월드 자동 초기화 강화
- 모듈 init 시 `start_world_agents`를 자동 호출하도록 변경
- sign_in 시 월드 생성 보장(`ensure_world_generated_in_dimension`) + npc stream projection 동기화
- 반영 파일:
  - `stitch-server/crates/game_server/src/init.rs`
  - `stitch-server/crates/game_server/src/auth/sign_in.rs`

### 6) 스폰 지점 walkable 보정
- transform 생성/차원 전환 시 nav grid로 walkable hex를 탐색해 스폰 좌표 선택
- 원점이 불가 시 ring 탐색으로 대체 위치 선택
- 반영 파일:
  - `stitch-server/crates/game_server/src/auth/mod.rs`

### 7) 구독 SQL 호환성 수정
- SpacetimeDB 구독 SQL에서 `BETWEEN` 사용 구문을 `>= / <=`로 전환
- AOI/World/V2/Building stream 전반 적용
- 반영 파일:
  - `stitch-server/crates/game_server/src/subscriptions/aoi.rs`
  - `stitch-server/crates/game_server/src/subscriptions/world_stream.rs`
  - `stitch-server/crates/game_server/src/subscriptions/v2_stream.rs`
  - `stitch-server/crates/game_server/src/subscriptions/building_stream.rs`

## 검증 결과

### 클라이언트
- `bun run typecheck` 통과
- `bun run build` 통과

### 서버
- `cargo check` 통과
- `cargo test` 통과 (31 passed, 0 failed)

### SpacetimeDB 재배포 및 데이터 확인
- 배포:
  - `spacetime build`
  - `spacetime publish --server 127.0.0.1:3000 stitch-server`
- 확인 SQL 결과:
  - `world_gen_params`: 1
  - `terrain_chunk_stream`: 49
  - `terrain_chunk_payload`: 49
  - `resource_node`: 134
  - `npc_state_stream`: 25

## 메모
- WebGPU 경고(`powerPreference ignored on Windows`)는 기능 장애가 아닌 브라우저/플랫폼 경고로 유지.
- 본 로그는 “기존 호환성 무시, 고성능 Orillusion 전환” 원칙 하에서 발생한 실변경/검증만 기록한다.

## 추가 반영 (지형 높이 정렬)

### 증상
- player/npc/building/resource가 terrain 높이와 무관한 고정 Y로 배치됨.

### 조치
- terrain payload에서 셀 높이를 샘플링하는 런타임 함수를 `WorldStreamVisualizer`에 추가.
- npc/resource/player/building 모두 `sampleTerrainHeight(x, z)` 기반으로 Y를 계산하도록 변경.
- `building_state` AOI 구독/렌더 경로를 추가.
- 로컬 플레이어는 `CharacterMotorComponent.snapToGround()` + `runtime.syncLocalPlayerGround()`로 프레임마다 지면 스냅.

### 반영 파일
- `stitch-orillusion-client/src/world/stream-visualizer.ts`
- `stitch-orillusion-client/src/net/aoi.ts`
- `stitch-orillusion-client/src/physics/character-motor-component.ts`
- `stitch-orillusion-client/src/app/runtime.ts`

### 재검증
- `bun run typecheck` 통과
- `bun run build` 통과

## 추가 반영 (geometry null 크래시)

### 증상
- 콘솔:
  - `TypeError: Cannot read properties of null (reading 'get')`
  - 경로: `MeshRenderer.geometry` 설정 중 `getAttribute` 실패
  - 지점: `WorldStreamVisualizer.syncNpcs`

### 조치
- `stream-visualizer`의 공유 geometry 사용을 제거하고, 오브젝트 생성 시 geometry를 개별 생성하도록 변경.
- 대상:
  - NPC (`SphereGeometry`)
  - resource/building/player/v2/fallback terrain (`BoxGeometry`)

### 반영 파일
- `stitch-orillusion-client/src/world/stream-visualizer.ts`

### 재검증
- `bun run typecheck` 통과
- `bun run build` 통과

## 추가 반영 (2026-02-21)

### 1) 월드 생성 진행 업데이트
- 대상: `IMPLEMENTDOC/15~20` 월드 생성 계획 기반 P1/P2/P3/P4 진척
- 반영: `cell_payload_version=2` 경로 정합성 보강, P2 reducer 정리, 성능 게이트 payload 산정 조정
- 상세 로그:
  - `IMPLEMENTDOC/logs/worldgen/2026-02-21-implementation-progress.md`

### 2) 반영 파일
- `stitch-server/crates/game_server/src/worldgen/mod.rs`
- `stitch-server/crates/game_server/src/services/nav.rs`
- `stitch-server/crates/game_server/src/reducers/worldgen/mod.rs`
- `stitch-orillusion-client/src/world/stream-visualizer.ts`
- `stitch-server/scripts/worldgen_perf_benchmark.sh`
- `stitch-server/scripts/worldgen_perf_thresholds.env`

### 3) 재확인
- `cargo check -p game_server`: 통과
- `bun run typecheck` (`stitch-orillusion-client`): 통과
