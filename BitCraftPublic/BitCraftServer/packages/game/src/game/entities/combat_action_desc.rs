use crate::CombatActionDescV3;

impl CombatActionDescV3 {
    /*
    pub can_use(&self, actor_id: u64) -> bool {

        ctx.db.action_state().owner_entity_id().find(&actor_id).filter
        if let Some(inventory) = InventoryState::get_with_index(&actor_id, 1) {

            if inventory.get_pocket_contents()
        }
    }
    */
}
