/**
 * Base class for all Spacetime host errors (i.e. errors that may be thrown
 * by database functions).
 *
 * Instances of SpacetimeError can be created with just an error code,
 * which will return the appropriate subclass instance.
 */
export declare class SpacetimeHostError extends Error {
    readonly code: number;
    readonly message: string;
    constructor(code: number, message?: string);
    get name(): string;
}
/**
 * An error thrown by a reducer that indicates a problem to the sender.
 *
 * When this error is thrown by a reducer, the sender will be notified
 * that the reducer failed gracefully with the given message.
 */
export declare class SenderError extends Error {
    constructor(message: string);
    get name(): string;
}
export declare const errors: Readonly<{
    readonly HostCallFailure: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
    readonly NotInTransaction: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
    readonly BsatnDecodeError: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
    readonly NoSuchTable: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
    readonly NoSuchIndex: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
    readonly NoSuchIter: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
    readonly NoSuchConsoleTimer: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
    readonly NoSuchBytes: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
    readonly NoSpace: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
    readonly BufferTooSmall: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
    readonly UniqueAlreadyExists: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
    readonly ScheduleAtDelayTooLong: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
    readonly IndexNotUnique: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
    readonly NoSuchRow: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
    readonly AutoIncOverflow: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
    readonly WouldBlockTransaction: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
    readonly TransactionNotAnonymous: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
    readonly TransactionIsReadOnly: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
    readonly TransactionIsMut: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
    readonly HttpError: {
        new (): {
            readonly code: number;
            readonly message: string;
            get name(): string;
            stack?: string;
            cause?: unknown;
        };
        CODE: 1 | 6 | 16 | 15 | 8 | 2 | 4 | 3 | 5 | 7 | 9 | 11 | 12 | 13 | 14 | 20 | 17 | 18 | 19 | 21;
        MESSAGE: "ABI called by host returned an error" | "ABI call can only be made while in a transaction" | "Couldn't decode the BSATN to the expected type" | "No such table" | "No such index" | "The provided row iterator is not valid" | "The provided console timer does not exist" | "The provided bytes source or sink is not valid" | "The provided sink has no more space left" | "The provided buffer is not large enough to store the data" | "Value with given unique identifier already exists" | "Specified delay in scheduling row was too long" | "The index was not unique" | "The row was not found, e.g., in an update call" | "The auto-increment sequence overflowed" | "Attempted async or blocking op while holding open a transaction" | "Not in an anonymous transaction. Called by a reducer?" | "ABI call can only be made while within a mutable transaction" | "ABI call can only be made while within a read-only transaction" | "The HTTP request failed";
        isError(error: unknown): error is Error;
        captureStackTrace(targetObject: object, constructorOpt?: Function): void;
        prepareStackTrace(err: Error, stackTraces: NodeJS.CallSite[]): any;
        stackTraceLimit: number;
    };
}>;
//# sourceMappingURL=errors.d.ts.map