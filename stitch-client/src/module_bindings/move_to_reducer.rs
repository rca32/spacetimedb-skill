// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct MoveToArgs {
    pub request_id: String,
    pub region_id: u64,
    pub client_ts_ms: u64,
    pub x: f32,
    pub y: f32,
    pub z: f32,
}

impl From<MoveToArgs> for super::Reducer {
    fn from(args: MoveToArgs) -> Self {
        Self::MoveTo {
            request_id: args.request_id,
            region_id: args.region_id,
            client_ts_ms: args.client_ts_ms,
            x: args.x,
            y: args.y,
            z: args.z,
        }
    }
}

impl __sdk::InModule for MoveToArgs {
    type Module = super::RemoteModule;
}

pub struct MoveToCallbackId(__sdk::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `move_to`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait move_to {
    /// Request that the remote module invoke the reducer `move_to` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_move_to`] callbacks.
    fn move_to(
        &self,
        request_id: String,
        region_id: u64,
        client_ts_ms: u64,
        x: f32,
        y: f32,
        z: f32,
    ) -> __sdk::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `move_to`.
    ///
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::ReducerEventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`MoveToCallbackId`] can be passed to [`Self::remove_on_move_to`]
    /// to cancel the callback.
    fn on_move_to(
        &self,
        callback: impl FnMut(&super::ReducerEventContext, &String, &u64, &u64, &f32, &f32, &f32)
            + Send
            + 'static,
    ) -> MoveToCallbackId;
    /// Cancel a callback previously registered by [`Self::on_move_to`],
    /// causing it not to run in the future.
    fn remove_on_move_to(&self, callback: MoveToCallbackId);
}

impl move_to for super::RemoteReducers {
    fn move_to(
        &self,
        request_id: String,
        region_id: u64,
        client_ts_ms: u64,
        x: f32,
        y: f32,
        z: f32,
    ) -> __sdk::Result<()> {
        self.imp.call_reducer(
            "move_to",
            MoveToArgs {
                request_id,
                region_id,
                client_ts_ms,
                x,
                y,
                z,
            },
        )
    }
    fn on_move_to(
        &self,
        mut callback: impl FnMut(&super::ReducerEventContext, &String, &u64, &u64, &f32, &f32, &f32)
            + Send
            + 'static,
    ) -> MoveToCallbackId {
        MoveToCallbackId(self.imp.on_reducer(
            "move_to",
            Box::new(move |ctx: &super::ReducerEventContext| {
                #[allow(irrefutable_let_patterns)]
                let super::ReducerEventContext {
                    event:
                        __sdk::ReducerEvent {
                            reducer:
                                super::Reducer::MoveTo {
                                    request_id,
                                    region_id,
                                    client_ts_ms,
                                    x,
                                    y,
                                    z,
                                },
                            ..
                        },
                    ..
                } = ctx
                else {
                    unreachable!()
                };
                callback(ctx, request_id, region_id, client_ts_ms, x, y, z)
            }),
        ))
    }
    fn remove_on_move_to(&self, callback: MoveToCallbackId) {
        self.imp.remove_on_reducer("move_to", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `move_to`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_move_to {
    /// Set the call-reducer flags for the reducer `move_to` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn move_to(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_move_to for super::SetReducerFlags {
    fn move_to(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("move_to", flags);
    }
}
