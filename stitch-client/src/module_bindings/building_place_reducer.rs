// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct BuildingPlaceArgs {
    pub building_id: u64,
    pub region_id: u64,
    pub hex_x: i32,
    pub hex_z: i32,
    pub required_item_def_id: u64,
    pub required_item_qty: u32,
    pub build_required: u32,
}

impl From<BuildingPlaceArgs> for super::Reducer {
    fn from(args: BuildingPlaceArgs) -> Self {
        Self::BuildingPlace {
            building_id: args.building_id,
            region_id: args.region_id,
            hex_x: args.hex_x,
            hex_z: args.hex_z,
            required_item_def_id: args.required_item_def_id,
            required_item_qty: args.required_item_qty,
            build_required: args.build_required,
        }
    }
}

impl __sdk::InModule for BuildingPlaceArgs {
    type Module = super::RemoteModule;
}

pub struct BuildingPlaceCallbackId(__sdk::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `building_place`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait building_place {
    /// Request that the remote module invoke the reducer `building_place` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_building_place`] callbacks.
    fn building_place(
        &self,
        building_id: u64,
        region_id: u64,
        hex_x: i32,
        hex_z: i32,
        required_item_def_id: u64,
        required_item_qty: u32,
        build_required: u32,
    ) -> __sdk::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `building_place`.
    ///
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::ReducerEventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`BuildingPlaceCallbackId`] can be passed to [`Self::remove_on_building_place`]
    /// to cancel the callback.
    fn on_building_place(
        &self,
        callback: impl FnMut(&super::ReducerEventContext, &u64, &u64, &i32, &i32, &u64, &u32, &u32)
            + Send
            + 'static,
    ) -> BuildingPlaceCallbackId;
    /// Cancel a callback previously registered by [`Self::on_building_place`],
    /// causing it not to run in the future.
    fn remove_on_building_place(&self, callback: BuildingPlaceCallbackId);
}

impl building_place for super::RemoteReducers {
    fn building_place(
        &self,
        building_id: u64,
        region_id: u64,
        hex_x: i32,
        hex_z: i32,
        required_item_def_id: u64,
        required_item_qty: u32,
        build_required: u32,
    ) -> __sdk::Result<()> {
        self.imp.call_reducer(
            "building_place",
            BuildingPlaceArgs {
                building_id,
                region_id,
                hex_x,
                hex_z,
                required_item_def_id,
                required_item_qty,
                build_required,
            },
        )
    }
    fn on_building_place(
        &self,
        mut callback: impl FnMut(&super::ReducerEventContext, &u64, &u64, &i32, &i32, &u64, &u32, &u32)
            + Send
            + 'static,
    ) -> BuildingPlaceCallbackId {
        BuildingPlaceCallbackId(self.imp.on_reducer(
            "building_place",
            Box::new(move |ctx: &super::ReducerEventContext| {
                #[allow(irrefutable_let_patterns)]
                let super::ReducerEventContext {
                    event:
                        __sdk::ReducerEvent {
                            reducer:
                                super::Reducer::BuildingPlace {
                                    building_id,
                                    region_id,
                                    hex_x,
                                    hex_z,
                                    required_item_def_id,
                                    required_item_qty,
                                    build_required,
                                },
                            ..
                        },
                    ..
                } = ctx
                else {
                    unreachable!()
                };
                callback(
                    ctx,
                    building_id,
                    region_id,
                    hex_x,
                    hex_z,
                    required_item_def_id,
                    required_item_qty,
                    build_required,
                )
            }),
        ))
    }
    fn remove_on_building_place(&self, callback: BuildingPlaceCallbackId) {
        self.imp.remove_on_reducer("building_place", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `building_place`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_building_place {
    /// Set the call-reducer flags for the reducer `building_place` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn building_place(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_building_place for super::SetReducerFlags {
    fn building_place(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("building_place", flags);
    }
}
